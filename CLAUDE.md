# SSMPD — لوحة إنتاج محتوى السوشيال ميديا لمركز Swnw

> اقرأ هذا الملف كاملاً قبل أي تعديل. أي شات جديد يشتغل على هذا المشروع
> لازم يبدأ من هنا.

---

## ما هذا المشروع

لوحة داخلية لتنظيم عمل إنتاج محتوى السوشيال ميديا لـ **مركز عيادات Swnw
التخصصية — فرع حدائق الأهرام**. أربعة أدوار (موظف صفحات، مصمم، مسؤول
اعتماد، سوبر أدمن) يشتغلوا على سير عمل واحد من الفكرة لحد النشر، مع
اعتماد مزدوج، كومنتات تعديل، أرشيف Google Drive تلقائي، وتحديث لحظي
(Realtime) بين كل الشاشات.

مشروع **مستقل تماماً** عن باقي أنظمة المركز (`sono-dashboard`، `hris`،
`swnw-erp`، `patients`) — ريبو منفصل + مشروع Supabase منفصل بالكامل.

منشور على GitHub Pages:
`https://mohamedabdelaalhub.github.io/SSMPD-Swnw-Social-Media-Prduction-Dashboard/`

---

## المستخدم

**محمد عبدالعال — غير تقني بالكامل.** لذلك:

- لا تُعطَ أوامر Terminal يكتبها بنفسه — أي خطوة تقنية تحتاج منه تُسلَّم
  كملف `.command` بالعربي يفتحه بدبل كليك، أو زرار يضغط عليه
- الشرح خطوة خطوة بالعربية المصرية
- عند أي خطأ: اطلب صورة الشاشة بدل الوصف
- خطوات تسجيل الدخول (GitHub / Supabase / Google) يعملها هو بنفسه في
  المتصفح — الشات يكمّل بعدها بالأتمتة

---

## البنية

موقع ثابت بالكامل — HTML/CSS/JavaScript عادي. **لا build ولا npm.**
الباك إند بالكامل Supabase (Auth + Postgres + Realtime).

```
index.html              نقطة الدخول — يحمّل كل الملفات بالترتيب
config.js                إعدادات المركز + مفاتيح Supabase + رابط جسر Drive
assets/css/styles.css    كل التصميم — خطوط وألوان الهوية
assets/fonts/            BigVesta Arabic + Hiragino Kaku (WOFF2)
assets/img/               logo.svg · mark.svg
assets/js/
  db.js                  طبقة الاتصال بـ Supabase (كل الاستعلامات من هنا)
  roles.js               الأدوار وصلاحيات كل تاب
  workflow.js             تعريف مراحل الـ Kanban وحالات أيقونة المصمم
  auth.js                 دخول/خروج/تسجيل عبر Supabase Auth
  drive.js                رفع ملفات التصميم لجسر Google Drive
  comments.js              مكوّن الكومنتات (Thread) + عداد "تعليق جديد" + حالة كل كومنت
  render-summary.js        شاشة الملخص العام
  render-production.js     شاشة إنتاج المحتوى (موظف الصفحات)
  render-review.js         شاشة إدارة المحتوى — Kanban (مسؤول الاعتماد)
  render-design.js         شاشة التصميم (المصمم) + رفع Drive
  render-publish.js        تاب النشر — المواد المعتمدة (محتوى+تصميم)، جدولة/نشر
  render-archive.js        شاشة الأرشيف — كالندر شهري
  render-admin.js          لوحة المستخدمين والصلاحيات (سوبر أدمن)
  app.js                   Bootstrap + الشِل العام + التنقّل + Realtime
supabase/setup.sql         سكريبت قاعدة البيانات كاملاً (RLS + Realtime)
apps-script/Code.gs        جسر أرشيف Google Drive (Apps Script Web App)
test/smoke.js              اختبار jsdom — Supabase مموّه بالكامل
```

كل ملف JS نمط **IIFE** يعلّق نفسه على `window.SSMPD*` — بدون أي تعارض
تسمية، وبدون أي build tool.

مكتبة خارجية واحدة من CDN: `@supabase/supabase-js@2`.

---

## قاعدة البيانات (Supabase)

مشروع Supabase منفصل بالكامل — **لا تخلطه بمشاريع الأنظمة الأربعة
التانية**. الجداول:

| الجدول | الوظيفة |
|---|---|
| `admins` | المستخدمين والأدوار (`page_manager`/`designer`/`approver`/`general_manager`/`super_admin`/`reception`/`customer_service`) + عمود `has_archive_access` (boolean منفصل عن الرول، بيفعّل الوصول لموديول أرشيف المرضى لأي رول) |
| `content_items` | كل مادة محتوى — العنوان والنص والمرحلة (`stage`)، `design_received_at` (وقت ضغط المصمم "استلام")، `scheduled_publish_at`/`scheduled_by` (معاد النشر المجدول ومين حددّه — تاب النشر)، `stage_history` (jsonb — سجل كل انتقالة مرحلة، أساس حساب أوقات الرحلة في تقرير الأرشيف) |
| `comments` | كومنتات التعديل، مرتبطة بالمادة كـ thread، وفيها `status` (`pending`/`done` — "في انتظار التعديل"/"تم التعديل") |
| `comment_reads` | آخر وقت قرا فيه كل مستخدم كومنتات كل مادة — أساس عداد "تعليق جديد" (أحمر/رمادي) |
| `activity_log` | سجل كل تغيير حالة (Audit trail) |
| `weekly_social_metrics` | إدخال يدوي أسبوعي لمؤشرات السوشيال ميديا (أو استيراد CSV من Meta Business Suite/Meta Ads/Google Ads عبر مطابقة أعمدة تلقائية في `render-summary.js`) |
| `ad_campaigns` | تقرير حملات إعلانات مدفوعة (Meta Ads Manager) مستورد يدوياً من CSV — كل استيراد جديد بيستبدل القديم بالكامل، صف واحد لكل حملة (مجمّع) |
| `patients` | **جدول مشترك** بين موديول "أرشيف المرضى" وموديول "إدارة الليدز" (قيد الإنشاء) — `patient_code` رقم تعريف قصير قابل للقراءة (`P-YYYY-000001`)، `national_id_hash` (هاش SHA-256+pepper بس، مش الرقم الخام)، `phone_normalized` للمطابقة مع الليدز |
| `patient_files` | metadata ملفات المرضى (الملف الفعلي على Google Drive عبر Service Account، مش هنا) — `category` (`id_document`/`insurance`/`radiology`/`lab_result`/`other`)، `checksum`، `drive_file_id` |
| `archive_access_log` | سجل كل عملية (`view`/`download`/`upload`/`delete`) على ملفات/بيانات المرضى — مين عملها وامتى |
| `leads` | (قيد الإنشاء) ليدز موديول "إدارة الليدز" — بيانات الرسالة الخام، الحالة (`current_status`)، التوزيع (`assigned_to`)، ربط اختياري بـ`patients` عبر `patient_id` |
| `lead_attempts` | (قيد الإنشاء) سجل كل محاولة تواصل مع الليد (متعدد لكل ليد) |
| `lead_status_log` | (قيد الإنشاء) سجل تغييرات `current_status` تلقائياً (بتريجر `trg_log_lead_status_change`) — أساس مؤشرات الأداء |
| `lead_feedback_tags` | (قيد الإنشاء) تصنيف اختياري (إيجابي/سلبي/محايد) لكل ليد أو محاولة تواصل |

**ثمان مراحل Kanban** (`content_items.stage`) — المفاتيح مخزّنة في القاعدة،
**لا تُغيَّر** بلا Migration:

```
idea_selection → initial_approval → in_design → final_approval
              → needs_revision (يرجع لـ initial_approval أو final_approval)
              → ready_to_publish → scheduled (اختياري — تاب النشر) → published
```

مرحلة `scheduled` اختيارية: من `ready_to_publish` ممكن تتنشر مباشرة
(تروح لـ`published`)، أو تتجدول الأول (تروح لـ`scheduled` بمعاد نشر
محدد `scheduled_publish_at`) ولحد ما حد يأكد إنها اتنشرت فعلاً (تتحول
لـ`published`)، أو تتلغي جدولتها (ترجع لـ`ready_to_publish`).

الحارس `guard_content_transition()` في `setup.sql` يمنع كل دور من تخطي
حدوده (مثلاً مصمم مايقدرش يعتمد نفسه، وموظف صفحات مايقدرش يلمس مادة
غيره). راجعه قبل أي تعديل على منطق الاعتماد.

RLS مفعّل على كل الجداول. `is_super()` و`my_role()` و`my_admin_id()`
دوال `SECURITY DEFINER` — بدونها السياسات تدخل في تكرار لا نهائي (نفس
الدرس اتعلّم في `sono-dashboard`).

**لتشغيل السكريبت أول مرة**: Supabase → SQL Editor → الصق `setup.sql`
كامل → عدّل البريد في القسم الأخير لو مختلف → Run.

---

## أرشيف Google Drive

القرار: **Apps Script Web App منشور من حساب Google الخاص بالمركز نفسه**
(مش حساب موظف). السبب: أبسط وأثبت من OAuth مباشر في موقع ثابت بدون
سيرفر، وبيضمن إن الأرشيف يفضل ملك المركز حتى لو الموظف اتغيّر.

**البنية على الدرايف** (فولدر جذر واحد مُشارَك، أنشأه المستخدم يدوياً
وشاركه معنا رابطه، وجوّاه 3 فولدرات ثابتة كل واحد بيتنظم سنة/شهر/يوم
تلقائياً):

```
SSMPD - Swnw Social Media Prduction Dashboard/   (الفولدر الجذر — من المستخدم)
  إنتاج المحتوى/     ← category: "content"   (id: 1dYiejCkw31-DP6SnWBo4KSOP1oRY3Wzq)
  التصميمات/         ← category: "design"    (id: 1E9OsjadaOUGc8asCzscrVHG5OFP_HVaS)  (الافتراضي)
  الأرشيف/           ← category: "archive"   (id: 1xghscimJG2f8CB2N3I0lC3fHrhZk2aMO)
```

الـ 3 فولدرات دي اتعملت مرة واحدة عبر الـ Google Drive MCP (مش سكريبت) —
**متتكررش تعمل فولدرات تانية بنفس الاسم**، استخدم نفس الـ IDs المذكورة.
لو الـ IDs دي اتغيّرت لأي سبب، حدّث `CATEGORY_FOLDER_IDS` في
`apps-script/Code.gs` (لازم إعادة نشر الـ Apps Script بعدها).

الكود في `apps-script/Code.gs` — بيستقبل الملف (base64) من `drive.js`
عبر `fetch(POST)` مع `category` ("content"/"design"/"archive")،
وبينشئ فولدرات `سنة / شهر / يوم` تلقائياً جوه فولدر النوع المناسب،
ويرفع الملف جواها، ويرجّع الرابط.

`drive.js` بيوفّر 3 دوال رفع ملفات: `uploadDesignFile` (مستخدمة فعلياً في
شاشة المصمم) و`uploadContentFile` و`uploadArchiveFile` (جاهزين للاستخدام
لو احتجنا رفع من شاشات تانية لاحقاً).

بعد نشر الـ Apps Script، الرابط يتحط في `config.js → driveBridge.webAppUrl`.

### ملفات التتبع (Excel) التلقائية — داخل كل فولدر نوع

كل فولدر من الـ 3 (إنتاج المحتوى/التصميمات/الأرشيف) فيه ملف Google Sheet
ثابت واحد بيتعمل أول مرة تلقائياً وبيتحدّث مع كل حدث في سير العمل:

- **تتبع - إنتاج المحتوى**: الفكرة، تاريخ الإضافة، الموقف الحالي (في
  الاعتماد/تم الاعتماد/تم التصميم/تم النشر)، لينك النشر.
- **تتبع - التصميمات**: عنوان التصميم، تاريخ ووقت الإرسال للتصميم، تاريخ
  ووقت رفع التصميم للاعتماد، تاريخ ووقت النشر، لينك النشر.
- **تقرير الأداء - الأرشيف**: صف واحد لكل مادة اتنشرت — رحلتها كاملة
  (فكرة → تصميم → نشر) + **إجمالي وقت الرحلة** + **وقت كل مرحلة على حدة**،
  محسوبين تلقائياً من `content_items.stage_history`.

المنطق كله في `apps-script/Code.gs` (`handleLog_`, `getOrCreateTrackingSheet_`,
`upsertTrackingRow_`, `computeDurations_`) و`drive.js` (`logIdea`,
`logDesignSent`, `logDesignUploaded`, `logPublished`) — الاستدعاءات
الفعلية في `render-production.js`/`render-review.js`/`render-design.js`
عند كل انتقالة مهمة. التسجيل دا **عملية خلفية غير حرجة**: لو الجسر لسه
مش متظبط أو حصل خطأ شبكة، بيتجاهل بهدوء وميوقفش سير العمل الأساسي.

⚠️ **ملاحظة ملكية**: الفولدرات الفرعية التلاتة اتعملت من حساب
`mohamadmh32@gmail.com` (المتصل بـ Claude) جوه فولدر جذر ملك
`swnwclinics@gmail.com` — مش مشكلة أمنية (الفولدر الجذر نفسه ملك حساب
المركز ومشارك)، لكن لو حبيت ملكية 100% لحساب المركز، ينشئوا يدوياً من
نفس الحساب بدل الاتنين دول.

---

## الهوية البصرية

نفس هوية Swnw في كل الأنظمة — **لا تغييرها بلا رجوع لملف الهوية
المركزي** (`Website/AGENTS.md`).

- الخطوط: `"Hiragino Kaku","BigVesta Arabic",system-ui,Tahoma,sans-serif`
  — ملفات WOFF2 محلية، ممنوع أي خط من Google Fonts
- الألوان: الأساسي `#0F369D` · التنبيه `#F15A22` · النص الداكن `#16212E`
  · الإيجابي `#2F7D5C` · السلبي `#D0402A`
- الاسم بالإنجليزي دايماً `Swnw` — لا `Sono` في أي نص يراه المستخدم

---

## ⚠️ قواعد لا تُكسر

1. **`config.js` لا تُعِد كتابته بالكامل** — فيه مفاتيح Supabase الحقيقية.
   عدّل سطراً بعينه فقط.
2. **بصمة الكاش `?v=N`** على كل `<script>`/`<link>` في `index.html` —
   ترفع مع أي تعديل JS/CSS، وإلا المستخدم هيشوف نسخة قديمة. الرقم الحالي: `23`.
3. **مفاتيح `stage` الثمانية ثابتة** — أي تغيير يكسر الحارس في SQL وكل
   منطق العرض.
4. **`guard_content_transition()`** هو مصدر الحقيقة لصلاحيات الانتقال —
   عدّله في `setup.sql` وطبّقه فعلياً في القاعدة، مش بس في الواجهة.
5. **الخصوصية**: هذا نظام محتوى تسويقي، لا علاقة له ببيانات المرضى —
   لا تُدخل أي بيانات مريض هنا أبداً.

---

## اختبار

بلا متصفح حقيقي في الساندبوكس → jsdom مع Supabase مموّه بالكامل:

```bash
npm install jsdom   # مرة واحدة
node test/smoke.js
```

يغطّي: شاشة الدخول، الشِل العام والتابات حسب الدور، الملخص العام،
الأرشيف (الكالندر)، Kanban السبع أعمدة.

**بعد أي تعديل**: شغّل `test/smoke.js`، وارفع بصمة الكاش، وتأكد يدوياً
من الرابط اللايف (سكرين شوت) قبل ما تقول للمستخدم "خلصت".

---

## حالة المشروع

**تم** (٢٠٢٦-٠٨-١٦): كل الشاشات الخمس + لوحة الأدمن + السكريبت الكامل
لقاعدة البيانات + جسر Drive + اختبار jsdom كامل.

**تم أيضاً** (٢٠٢٦-٠٨-١٦، دفعة تانية — `?v=4`):
- زرار "تعليق جديد" + عداد أحمر/رمادي بجانب "فتح" في كل الشاشات
  (`comment_reads` + `comments.js`)
- اسم دور المصمم بقى "مصمم جرافيك"
- "تم الاستلام" بقت زرار "استلام" فعلي (`design_received_at`)
- العنوان في الجداول بقى قابل للضغط لفتح التفاصيل (`link-open`)
- اسم المستخدم الحقيقي بيظهر فوق كل كومنت (بدل "مستخدم")
- زرار حالة لكل كومنت: "في انتظار التعديل" / "تم التعديل" (`comments.status`)
- "اتاعتمدت" اتغيّرت لـ"المواد المعتمدة" في شاشة التصميم
- Kanban بقى `flex-wrap` عشان كل الـ 7 أعمدة (بما فيها "تم النشر") تظهر
  من غير سكرول أفقي مخفي
- استيراد CSV اختياري (Meta Business Suite / Meta Ads / Google Ads) في
  مودال بيانات الأسبوع بالملخص العام — بيحاول يلاقي أعمدة الوصول/التفاعل/المتابعين
  الجدد بمطابقة كلمات مفتاحية، ويعبّي الخانات تلقائياً **للمراجعة قبل الحفظ**
  (مش حفظ مباشر، لأن شكل الملف بيختلف حسب المصدر)
- ملفات Excel تتبّع تلقائية جوه كل فولدر Drive (شرح كامل فوق في قسم
  "ملفات التتبع")
- جسر Google Drive اتنشر فعلياً من حساب المركز (`driveBridge.webAppUrl`
  في `config.js` بقى رابط حقيقي مش placeholder)

**تم أيضاً** (٢٠٢٦-٠٨-١٧، دفعة تالتة — `?v=5`):
- زرار "تعليق جديد" بقى ميظهرش خالص لو مفيش كومنتات على المادة (مش بس
  الرقم بيختفي — الزرار كله)
- تمييز **سونو / د.دينا** على مستوى الداشبورد كلها: `content_items.brand`
  (عمود موجود بالفعل من دفعة SQL السابقة) بقى له واجهة كاملة —
  - منشئ المحتوى بيحدد الصفحة إجباري عند إنشاء أي مادة جديدة
    (`render-production.js` → `openCreateModal`)
  - بادچ ملوّن بجانب العنوان في كل الشاشات (إنتاج المحتوى، إدارة
    المحتوى/Kanban، التصميم، الأرشيف) — `workflow.js → brandBadgeHtml`
  - عند النشر (من أي شاشة) بيتم اختيار الصفحة والمنصة تاني (دروب داون:
    فيسبوك/انستجرام/تيكتوك/قناة يوتيوب/الموقع الإلكتروني) —
    `content_items.publish_platform`
- سكشن جديد **"جاهز للنشر"** في شاشة "إدارة المحتوى" (`render-review.js`)
  — منفصل عن Kanban، بيعرض كل مادة اعتمدت نهائياً مع دروب داون الصفحة
  والمنصة وخانة رابط النشر وزرار "نشر" مباشر (يستخدم صلاحية الاعتماد
  الجديدة `ready_to_publish → published` للـ approver المضافة في
  `guard_content_transition()` قبل كده)
- شاشة "إنتاج المحتوى" (`render-production.js`) بردو بقى فيها نفس
  اختيار الصفحة/المنصة وقت النشر في `openPublishModal` (لأن موظف
  الصفحات كمان عنده صلاحية نشر مباشرة من شاشته)

**تصحيح مهم** (٢٠٢٦-٠٨-١٧ — `?v=7`): إصلاح باگ كان يمنع أي موظف جديد
من الدخول نهائياً بعد التسجيل، حتى لو السوبر أدمن ضافه فعلاً من قبل.
السبب: جدول `admins` وسياسات RLS (`read own or pending` و`claim own
invite` في `setup.sql`) كانوا مُجهّزين من الأول لدعم "دعوة معلّقة"
(صف بالإيميل بس، `user_id` فاضي، لحد ما الموظف يعمل حساب لأول مرة)،
لكن دالتَي `getPendingInviteByEmail`/`claimInvite` في `db.js` **ماكانوش
متوصّلين بأي حاجة في الواجهة** — أي موظف جديد كان بيتسجل بنجاح لكن
`loadCurrentAdmin` بيدور بس بـ`user_id` (لسه فاضي في صفه)، فيفشل
برسالة "هذا البريد غير مُضاف" حتى لو صفه موجود فعلاً. الإصلاح في
`auth.js` (`loadCurrentAdmin` بقى ياخد الإيميل كمان، ولو مفيش صف
بالـ`user_id` بيدور على دعوة معلّقة بنفس الإيميل ويربطها) و`app.js`
(بيمرر الإيميل)، و`db.js` (المقارنة بقت case-insensitive بـ`ilike`
عشان تتطابق مع `lower(email)` في سياسة RLS). مغطّى الآن باختبار في
`test/smoke.js`.

**تم أيضاً** (٢٠٢٦-٠٨-١٧ — `?v=8`):
- في مودال مراجعة/اعتماد المادة بشاشة "إدارة المحتوى" (`render-review.js`
  → `openReviewModal`)، بقى فيه دروب داون + زرار "حفظ" لتعديل الصفحة
  (سونو/د.دينا) في أي وقت — مش بس وقت الإنشاء أو النشر — لو حصل غلط
  والمسؤول محتاج يصححها

**تم أيضاً** (٢٠٢٦-٠٨-١٧ — `?v=9`):
- **اوبشن تعديل/حذف على كل مادة محتوى**، ظاهر في كل الشاشات اللي بتفتح
  تفاصيل مادة (إنتاج المحتوى، إدارة المحتوى/Kanban، شاشة التصميم،
  الأرشيف) — منطق مشترك في `workflow.js`:
  - `W.canEditItem(me, item)`: السوبر أدمن يقدر يعدّل أي مادة، وموظف
    الصفحات يقدر يعدّل بس المادة اللي هو نفسه أنشأها
  - `W.canDeleteItem(me)`: الحذف مقصور على السوبر أدمن بس — ده مطابق
    فعلياً لصلاحية RLS `"super deletes content"` في `setup.sql` (كانت
    موجودة من الأول بس مفيش زرار في الواجهة كان بيستخدمها)
  - "تعديل" بيفتح مودال بسيط لتغيير العنوان/النص/الصفحة
    (`W.openEditContentModal`)
  - "حذف" بيطلب تأكيد ثم حذف نهائي (`W.deleteContentItemWithConfirm` +
    `db.js → deleteContentItem`) — الإجراء ده **لا يمكن التراجع عنه**
- **ريفريش تلقائي أقوى** في `app.js`: بالإضافة للاشتراك اللحظي الموجود
  على `content_items`، بقى فيه اشتراك لحظي كمان على جدول `comments`
  (عشان الكومنتات الجديدة تظهر تلقائياً)، + نسخة احتياطية: ريفريش دوري
  كل ٤٥ ثانية (`setInterval`) لو الاتصال اللحظي (WebSocket) انقطع أو
  اتأخر لأي سبب — بيشتغل بس لو مفيش مودال مفتوح دلوقتي عشان ميقطعش حد
  وهو بيكتب

**تم أيضاً** (٢٠٢٦-٠٨-١٧ — `?v=10`):
- **دور جديد "مدير عام" (`general_manager`)**: له كل صلاحيات المحتوى زي
  السوبر أدمن بالظبط (اعتماد، تصميم، إنشاء/تعديل/حذف أي مادة، نشر، إدخال
  مؤشرات الأسبوع) — **لكن من غير تاب "⚙ المستخدمون" خالص** (مش قادر
  يضيف/يعدّل/يحذف أي مستخدم أو يغيّر دور حد). التنفيذ:
  - `roles.js`: الدور مضاف لـ`ROLES` ولكل التابات في `TAB_ACCESS` ما عدا
    `admin`، و`canApprove`/`canDesign`/`canCreateContent` بقوا يرجّعوا
    `true` للمدير العام برضه
  - `workflow.js`: `canEditItem`/`canDeleteItem` بقوا يسمحوا للمدير العام
  - `setup.sql`: دالة جديدة `can_manage_all_content()` (سوبر أدمن أو مدير
    عام) بتستخدمها `guard_content_transition()` (تخطي كامل لحارس
    المراحل، زي `is_super()` بالظبط) وسياسة حذف `content_items` وسياسة
    إدخال مؤشرات الأسبوع — أما سياسات جدول `admins` نفسه (`super reads
    all`, `super writes all`, `guard_admin_changes`) فضلت على `is_super()`
    لوحدها **عمداً** عشان المدير العام ميقدرش يلمس المستخدمين. لازم
    السوبر أدمن يشغّل `setup.sql` تاني في Supabase SQL Editor عشان
    الدور الجديد يتفعّل في القاعدة (آمن للتشغيل أكتر من مرة).

**تم أيضاً** (٢٠٢٦-٠٨-١٧ — `?v=11`):
- **تاب جديد "النشر"** — كل مادة خلصت اعتماد نهائي وتصميم (محتوى معتمد +
  تصميم معتمد سوا في نفس الكارت) بتظهر هنا بدل ما تتفرق بين شاشتين.
  الوصول: موظف الصفحات، مسؤول الاعتماد، المدير العام، السوبر أدمن —
  **المصمم ممنوع** (زي ما طلب المستخدم بالظبط، `roles.js → TAB_ACCESS`)
- **مرحلة Kanban جديدة `scheduled`** ("مجدولة للنشر") بين `ready_to_publish`
  و`published` — جدولة حقيقية بتاريخ ومعاد (`content_items.scheduled_publish_at`
  + `scheduled_by`)، مش مجرد قائمة نشر فوري:
  - من تاب النشر: مادة "جاهزة للنشر" ممكن تتجدول (تختار الصفحة/المنصة/معاد
    النشر → تروح لحالة "مجدولة") أو تتنشر فوراً من غير جدولة (تدخل رابط
    المنشور مباشرة)
  - مادة "مجدولة" ممكن "تأكيد النشر" (تدخل رابط المنشور، تتحول لـ`published`)
    أو "إلغاء الجدولة" (ترجع لـ`ready_to_publish` وتتمسح بيانات الجدولة)
  - `guard_content_transition()` في `setup.sql` اتحدّث (لموظف الصفحات
    ومسؤول الاعتماد) يسمح بالانتقالات: `ready_to_publish→scheduled`،
    `scheduled→published`، `scheduled→ready_to_publish`
- **اتشالت السكشنات القديمة "جاهز للنشر"** من شاشتي "إدارة المحتوى"
  (`render-review.js`) و"إنتاج المحتوى" (`render-production.js`) نهائياً
  — بقت كلها مجمّعة في تاب "النشر" الجديد بس (مفيش تكرار)
- **إصلاح باگ في `comments.js`**: كان بيستخدم `id` ثابت غير فريد
  (`new-comment-box`/`send-comment-btn`) بيتلاقى بـ`document.getElementById`
  — كان شغال تمام لما شاشة واحدة بتفتح ثريد كومنتات واحد بس في مودال،
  لكن تاب النشر الجديد بيعرض عدة كروت مواد في نفس الوقت وكل واحد له
  ثريد كومنتات مفتوح جنبه (مش في مودال) — فبقى لازم `class` بدل `id`
  مع `container.querySelector(...)` عشان كل ثريد يشتغل صح لوحده

**تم أيضاً** (٢٠٢٦-٠٨-١٧ — `?v=12`):
- **سكشن جديد "الإعلانات المدفوعة (Meta Ads)"** في شاشة الملخص العام
  (`render-summary.js`) — منفصل عن سكشن مؤشرات السوشيال ميديا الأسبوعية
  اليدوية، لأنه بيانات حملات إعلانات مدفوعة تراكمية (مش أسبوعية):
  - جدول جديد `ad_campaigns` في `setup.sql` — صف واحد لكل حملة (اسم
    الحملة، المبلغ المُنفق، مرات الظهور، الوصول، النتائج ومؤشرها، تكلفة
    النتيجة، النقرات، نسبة النقر، فترة التقرير)
  - زرار "استيراد تقرير حملات إعلانات" (لمسؤول الاعتماد/المدير
    العام/السوبر أدمن فقط، زي صلاحية إدخال مؤشرات الأسبوع) — بيرفع ملف
    CSV تصدير Campaigns من Meta Ads Manager، ويطابق أعمدته بتطابق تام
    لاسم العمود (مش كلمات مفتاحية زي مؤشرات الأسبوع، لأن تصدير Meta Ads
    نفسه أعمدته ثابتة ومعروفة)، ويجمّع الصفوف لكل حملة (لو نفس الحملة
    ظهرت أكتر من صف لأن التصدير بيبقى على مستوى مجموعة الإعلان)
  - كل استيراد جديد **بيستبدل** التقرير القديم بالكامل (مش تراكمي زي
    مؤشرات الأسبوع) — مناسب لأنه تقرير حملات شامل من تاريخ لتاريخ، مش
    إدخال أسبوعي منفصل
  - السكشن بيعرض ٤ مؤشرات مجمّعة (إجمالي المبلغ المُنفق، الوصول،
    النقرات، متوسط نسبة النقر) + جدول تفصيلي لكل حملة

**تم أيضاً** (٢٠٢٦-٠٨-١٧ — `?v=13`): دفعة إصلاحات UX بناءً على بلاغات المستخدم
- **أرقام سكشن "الإعلانات المدفوعة" بقت إنجليزي دايماً**: `fmtNum()` في
  `render-summary.js` كان بيستخدم `toLocaleString("ar-EG")` (أرقام هندية) —
  اتغيّر لـ`"en-US"` بناءً على طلب صريح من المستخدم
- **قائمة موبايل/تابلت (Hamburger menu)**: على الشاشات ≤٨٦٠px (موبايل
  والآيباد بالوضع الرأسي) شريط التابات واسم المستخدم بيتخفوا ويظهر بدالهم
  زرار واحد (اسم المستخدم + سهم) في `renderShell()` بـ`app.js` — بالضغط
  عليه بتفتح قائمة منسدلة بالترتيب اللي المستخدم طلبه بالظبط: الاسم، ثم
  الدور، ثم قائمة التابات، ثم زرار خروج. عناصر التابات نفسها (`.tab-btn`)
  اتكررت (نسخة في الشريط العادي + نسخة جوه القائمة) عشان نفس منطق التنقّل
  والـ`active` state يشتغلوا من غير تكرار كود
- **استجابة الموبايل/التابلت بشكل عام** (`styles.css`): جداول `table.simple`
  بقت تعمل سكرول أفقي جوه الـ`.section` نفسها بدل ما تتكسر، مؤشرات KPI
  وكروت وكالندر الأرشيف بقى لهم أحجام/تباعد مناسبة على شاشات أصغر
  (breakpoints عند ٨٦٠/٧٢٠/٤٨٠px)
- **الريفريش التلقائي بقى أذكى** (`app.js`):
  - بيتذكّر آخر تاب كان مفتوح في `sessionStorage` ويرجّع له بعد أي ريفريش
    فعلي للصفحة (F5) بدل ما يرجع دايماً لتاب البداية الافتراضي حسب الدور
  - `refreshCurrentTab()` بقى عنده `isUserEditing()` — بيتأكد إن مفيش حقل
    نص/تاريخ متعبّى أو فوكس عليه في الشاشة الحالية (كومنت لسه ما اتبعتش،
    فورم جدولة نشر...) قبل ما يعمل إعادة رسم كاملة، عشان الريفريش
    اللحظي/الدوري (كل ٤٥ ثانية أو أي تغيير Realtime) ميمسحش حاجة المستخدم
    لسه بيكتبها
- **بديل لـ`alert()`/`confirm()` المتصفح في تاب النشر** (`render-publish.js`
  + `toast.js` جديد): كل رسائل التحقق والأخطاء والنجاح بقت `toast` غير
  بلوكينج بدل نافذة `alert()`، وإلغاء الجدولة بقى تأكيد بضغطة تانية على
  نفس الزرار بدل `confirm()`. السبب: `alert()`/`confirm()` ممكن يتمنعوا
  أو يتجاهلوا جوه متصفحات مدمجة في تطبيقات موبايل (واتساب/ماسنجر) —
  فالمستخدم كان بيحس إن زرار "تأكيد النشر" مثلاً "مش عامل حاجة" مع إنه في
  الواقع كان بيستنى تعبئة رابط المنشور، ومفيش رسالة واضحة توصله. (تأكدنا
  عن طريق تكرار فعلي على الموقع المنشور: النشر والجدولة فعلياً بيوصلوا
  للملخص والأرشيف صح لما الفورم بيتعبّى كامل — الباگ كان في وضوح رسالة
  التحقق مش في عدم وصول البيانات)

## موديولين جداد قيد الإنشاء (٢٠٢٦-٠٨-١٧ وما بعدها): أرشيف المرضى + إدارة الليدز

بدأنا بناء قطاعين كاملين جداد جنب SSMPD (وصول مقصور على مستخدمين محددين لكل
قطاع، بنفس نظام الأدوار/الصلاحيات الحالي مش نظام منفصل):

- **أرشيف المرضى**: رفع/إدارة ملفات المرضى (تحقيق شخصية/تأمين/أشعة/تحاليل)
  على Google Drive عبر Service Account، مقصور على `has_archive_access = true`.
- **إدارة الليدز والتواصل مع العملاء**: تحويل دورة استقبال رسائل واتساب/ماسنجر
  اليدوية (إكسيل) لنظام آلي، برولين جداد `reception`/`customer_service`.

**قرار معماري مهم**: الداشبورد كانت لحد دلوقتي static بالكامل (GitHub Pages)
من غير أي backend — بس المتطلبات الأمنية للموديولين دول (مفتاح Google Service
Account، والتحقق من JWT قبل أي عملية) محتاجة backend حقيقي. **القرار: Supabase
Edge Functions** (بدل سيرفر Node منفصل) — نفس المنصة المستخدمة بالفعل،
الـ secrets بتتدار رسمياً (`supabase secrets set`)، ومفيش استضافة إضافية.
الفرونت‌إند هيفضل static زي ما هو وبس هيكلم روابط `https://<project>.supabase.co/functions/v1/...`.

**أمان — درس اتعلمناه من "SSMPD Bridge"**: ممنوع تماماً نفس نمط "رابط بدون
auth" (`Anyone with the link`). كل اتصال بـ Google Drive للموديولين دول لازم
يعدّي من Edge Function بتتحقق من JWT بتاع Supabase Auth ومن الصلاحية المناسبة
(`has_archive_access` أو رول من رولات الليدز) قبل أي عملية. مفتاح الـ Service
Account **ميظهرش في أي كود frontend إطلاقاً**، وبيتخزن كـ Supabase secret بس.

**المرحلة ١ اللي خلصت — الجدول المشترك (schema فقط)**:
- جدول `patients` المشترك بين الموديولين + `patient_files` + `archive_access_log`
  (تفاصيلهم في جدول الجداول فوق)
- رولين جداد `reception`/`customer_service` في `admins.role`
- عمود `admins.has_archive_access` منفصل عن الرول
- دالتين صلاحية جداد: `public.has_archive_access()` و`public.can_access_leads()`
  (بنفس أسلوب `my_role()`/`is_super()` الموجودين)، مستخدمين في RLS الجداول التلاتة

**المرحلة ٢ اللي خلصت — schema تفصيلي لموديول الليدز (٢٠٢٦-٠٨-١٧)**:
- 4 جداول جداد: `leads`، `lead_attempts` (سجل محاولات تواصل متعدد)،
  `lead_status_log` (سجل تغييرات حالة تلقائي)، `lead_feedback_tags` (تقييم خدمة)
- تريجر `trg_log_lead_status_change` بيسجل أي تغيير في `leads.current_status`
  في `lead_status_log` تلقائياً (`security definer`) — بدون الاعتماد على إن
  الموظف يسجله يدوياً
- RLS دقيقة على مستوى الصف: خدمة العملاء تشوف/تعدّل بس الليدز المُسندة لها
  (`assigned_to = my_admin_id()`)، الاستقبال قراءة كاملة + إضافة بس (لازمة
  لكشف التكرار وقت الإدخال)، المدير/السوبر أدمن كل حاجة — حسب قرار الصلاحيات
  المُتفق عليه في السبيسيفيكيشن الأصلية
- منطق التطبيق نفسه (تطبيع الأرقام، كشف التكرار، الربط التلقائي بـ`patients`،
  التوزيع التلقائي، تنبيهات SLA) **لسه مش متنفذ** — دي مسؤولية مرحلة Edge
  Functions الجاية، السكيما هنا جهزت بس الأرضية

**المرحلة ٣ اللي خلصت — Edge Functions لموديولين (٢٠٢٦-٠٨-١٨)**:

كل الـ Edge Functions اتكتبت ونُشرت فعلياً على Supabase (`supabase/functions/`)
وبتشتغل حالياً — مفيش أي حاجة "شغالة نظرياً بس". النمط المشترك بين كل
دوال الموديولين: تحقق من JWT بتاع Supabase Auth، تحقق من صلاحية/رول
المستخدم، عملية على القاعدة أو Google Drive، رجوع JSON. مفيش أي مسار
عام (public) بدون auth.

*أرشيف المرضى* (مقصور على `has_archive_access` أو `super_admin`):
- `patients-create`: إنشاء مريض جديد — تطبيع الهاتف، هاش الرقم القومي
  (SHA-256 + `NATIONAL_ID_PEPPER`)، `patient_code` تلقائي من القاعدة
- `patient-files-upload`: رفع ملف مريض لـ Google Drive عبر Service
  Account (`GOOGLE_SERVICE_ACCOUNT_KEY`) بـ resumable upload حقيقي (مش
  base64) — بينشئ فولدر المريض وفولدر الفئة تلقائياً، بيحسب checksum،
  وبيسجل `archive_access_log`
- `patient-files-list`: بحث/قائمة مرضى (صفحات)، أو ملفات مريض بعينه
  (`?patient_id=`) مع تسجيل "view" في اللوج
- `patient-files-download`: تنزيل ملف عبر بروكسي من السيرفر (مش رابط
  درايف مباشر) — الموظف مايشوفش أي share link خالص
- `patient-files-delete`: حذف ملف من Drive + القاعدة، مقصور على
  `super_admin`/صاحب صلاحية الأرشيف الكاملة

*إدارة الليدز* (لـ`reception`/`customer_service`/`general_manager`/`super_admin`):
- `leads-create`: إنشاء ليد من رسالة واتساب/ماسنجر — نفس منطق تطبيع
  الهاتف بالظبط زي `patients-create` (عشان المطابقة تشتغل)، كشف تكرار
  (ليد مفتوح بنفس الرقم → 409 إلا لو `confirm_duplicate:true` أو
  `link_to_lead_id` للربط بدل الإنشاء)، مطابقة تلقائية مع `patients`
  (جديد/قديم)، توزيع تلقائي على أقل موظف `customer_service` عدد ليدز
  مفتوحة حالياً
- `leads-list`: قائمة ليدز مع فلترة/بحث/صفحات — خدمة العملاء تشوف بس
  الليدز المُسندة لها (فلتر إضافي فوق RLS)
- `leads-attempt`: تسجيل محاولة تواصل (`lead_attempts` — القيم المسموحة
  `answered`/`no_answer`/`busy`/`call_back_later`/`other` زي الـ check
  constraint بالظبط)، بيرقّي الحالة تلقائياً من `new` لـ`in_progress`
  عند أول محاولة، وبيحدّث `next_follow_up_date` لو اتبعت
- `leads-update-status`: تغيير `current_status`/`priority`/
  `booking_reference`/`do_not_contact` — تريجر `trg_log_lead_status_change`
  الموجود بالفعل بيسجل أي تغيير حالة تلقائياً في `lead_status_log`،
  مفيش داعي نسجله يدوي في الدالة

**Secrets المُدخلة في Supabase** (Edge Function Secrets — المستخدم
دخلهم بنفسه، محدش شافهم في أي كود أو شات): `GOOGLE_SERVICE_ACCOUNT_KEY`،
`NATIONAL_ID_PEPPER`. + `DRIVE_PATIENT_ARCHIVE_FOLDER_ID` و
`DRIVE_LEADS_REPORTS_FOLDER_ID` (IDs فولدرات عادية، مش سرية).

**المرحلة ٤ اللي خلصت — الشاشات الحقيقية الكاملة لموديولين (٢٠٢٦-٠٨-١٨ — `?v=15`)**:

الموديولين بقوا شغالين بكامل وظائفهم على الداشبورد (مش mockup) — كل تاب
جديد بيستخدم نفس بنية `.tabs`/`.tab-btn` الموجودة كـ"تنقّل فرعي" داخلي
جوه تاب واحد رئيسي، بدل ما يتلمس `app.js`/`RENDERERS`/`TAB_LABELS` الأصليين:

- **أرشيف المرضى** (`render-patients.js`) — ٤ شاشات فرعية: بحث/قائمة
  مرضى، ملف مريض (رفع/تنزيل/حذف ملفات)، طابور مراجعة الملفات (اعتماد/رفض
  مع سبب اختياري)، لوحة إحصائيات. رفض الملف بقى `input` مضمّن جوه صف
  الجدول (مش `prompt()` المتصفح) اتساقاً مع قاعدة `?v=13` (المتصفحات
  المدمجة زي واتساب/ماسنجر ممكن تمنع `prompt()`/`alert()`/`confirm()`)
- **إدارة الليدز** (`render-leads.js`) — ٦ شاشات فرعية حسب الدور:
  الاستقبال (إدخال ليد جديد + كشف تكرار)، خدمة العملاء (الليدز المُسندة
  بس)، داشبورد الإدارة (إحصائيات)، الحجوزات الفعلية، أرشيف الليدز الكامل
  (فلترة/بحث)، ورفع ملف إكسيل/CSV جماعي (`bulkCreateLeads` + مكتبة
  SheetJS من CDN — أضيفت لـ`index.html`). مودال تفاصيل الليد بيعرض سجل
  محاولات التواصل (مين اتصل وحالة الليد وقتها) + سجل تغييرات الحالة
  (Audit trail: مين غيّر ومتى) + قسم رفع/عرض فواتير الحجز
- **تصحيح باگ مُبلّغ عنه**: مودال تفاصيل الليد كان مش بيحدّث شكل "دائرة
  الحالة" بصرياً فوراً بعد تغيير الحالة من الدروب داون. السبب: زرار
  الحفظ كان بيقفل المودال (`backdrop.remove()`) بدل ما يعيد رسمه. الحل:
  بقى بيعمل `reload()` (إعادة جلب بيانات الليد وإعادة رسم نفس المودال في
  مكانه) فتظهر كل التحديثات (الدائرة + السجلات) فوراً من غير قفل/فتح
- **تصحيح فجوة بيانات**: `uploaded_by_name`/`reviewed_by_name` في شاشة
  ملف المريض ومراجعة الملفات كانوا فاضيين دايماً (الحقل مكانش بيترجع
  أصلاً من `patient-files-list`) — اتصلح بإضافة joins
  `admins!uploaded_by(name)`/`admins!reviewed_by(name)` في الـ Edge
  Function
- **تصحيح فجوة أمنية/بيانات أهم — سجل "مين عدّل ومتى" كان دايماً فاضي
  لليدز**: أثناء بناء عرض الـ Audit trail لاحظنا إن `changed_by` في
  `lead_status_log` و`booked_by` في `leads` كانوا دايماً `NULL` لأي
  تحديث جاي من التطبيق. السبب: كل تحديثات الليدز بتعدّي من Edge Function
  بمفتاح `service_role`، ودالة `my_admin_id()` (اللي التريجرز بتعتمد
  عليها لمعرفة "مين ده") بترجع بس لو فيه `auth.uid()` (جلسة مستخدم
  حقيقية) — مش موجودة خالص في سياق service role. الحل: دالة جديدة
  `effective_admin_id()` بتشيك أول حاجة على متغيّر ترانزاكشن محلي
  (`app.caller_admin_id`)، ودالة RPC جديدة `rpc_update_lead()` بتحدد
  المتغيّر ده بمعرّف المستخدم الحقيقي (اللي Edge Function عارفه من الـ
  JWT) قبل ما تعمل الـ`update` — كل التريجرز اتحدّثت تستخدم
  `effective_admin_id()` بدل `my_admin_id()`. `leads-update-status`
  بقت بتنادي `rpc_update_lead()` بدل `.update()` مباشرة. **لازم يتشغّل
  `setup.sql` تاني في Supabase SQL Editor لو حد بيطبّق النسخة دي على
  قاعدة تانية** (آمن للتشغيل أكتر من مرة زي العادة)
- بصمة الكاش اترفعت لـ `?v=15` في `index.html` (كل ملفات JS/CSS + إضافة
  سكريبت SheetJS الجديد)

**تصحيح أمني مهم — إصلاح خطأ رفع فواتير الليدز وملفات المرضى (٢٠٢٦-٠٨-١٨ — `?v=16`)**:

- **المشكلة**: رفع فاتورة حجز على ليد كان بيفشل برسالة `403
  storageQuotaExceeded` من جوجل درايف. السبب: `patient-files-upload`
  و`lead-invoice-upload` كانوا بيرفعوا الملفات مباشرة بحساب Service
  Account، واللي زي ما موثّق فوق **مفيش عنده مساحة تخزين شخصية خالص** —
  نفس المشكلة اللي كانت اتحلت قبل كده لجسر "SSMPD Bridge" الأصلي.
- **الحل**: الدالتين اتعاد كتابتهم بالكامل عشان يرفعوا الملف عن طريق نفس
  جسر Google Apps Script (`apps-script/Code.gs`) اللي بيشتغل بحساب
  المركز الحقيقي (`swnwclinics@gmail.com`) وعنده مساحة تخزين حقيقية —
  بالظبط زي ما `drive.js` بيعمل للفرونت‌إند من الأول. الجسر اتحدّث
  (`CATEGORY_FOLDER_IDS` + `handlePatientArchiveUpload_` +
  `handleLeadsInvoiceUpload_`) عشان يستقبل فئتين جداد `patient_archive`
  و`leads_invoice` — الفولدرات دي **Restricted** (مش Public زي فولدرات
  المحتوى/التصميم/الأرشيف الأصلية) ومش مستخدمة من الفرونت‌إند العام
  خالص، بس من الـ Edge Functions اللي بتتحقق من JWT والصلاحية الأول —
  فالحماية اللي كانت موجودة (`has_archive_access`/رولات الليدز) فضلت
  زي ما هي بالظبط.
- **تحسين إضافي**: رفع فاتورة الليد بقى يقبل **صور + PDF + إكسيل**
  (مش صور بس زي الأول) — `isAllowedMime()` في `lead-invoice-upload`
  و`accept="image/*,.pdf,.xlsx,.xls"` في حقل رفع الفاتورة بـ
  `render-leads.js`.
- بصمة الكاش اترفعت لـ `?v=16` في `index.html`.

**تم أيضاً — قائمة منسدلة موحّدة + رفع مستقل لكل تصنيف + تغيير كلمة السر
(٢٠٢٦-٠٨-١٨ — `?v=17`)**:

- **قائمة منسدلة موحّدة** (أيقونة ☰ بجانب الاسم في الشريط العلوي، بتظهر
  في كل أحجام الشاشات وليها زر خاص بها بعيد عن قائمة الموبايل الحالية):
  بالترتيب اللي طلبه المستخدم بالظبط — الاسم، الدور، **SSMPD** (يرجع
  لأول تاب من تابات SSMPD الأساسية المسموحة للدور)، **أرشيف المرضى**،
  **إدارة الليدز والتواصل**، **تغيير كلمة السر**، **لوحة التحكم** (الاسم
  الجديد لتاب "⚙ المستخدمون" — نفس التاب، غيّرنا بس الاسم الظاهر)، خروج.
  العناصر بتظهر/تتخفي حسب صلاحيات الدور (`app.js → renderShell`، كلاس
  CSS جديد `.user-dropdown`).
- **رأس موحّد لكل موديول منفصل**: شاشتَي "أرشيف المرضى" و"إدارة الليدز
  والتواصل" بقى ليهم عنوان علوي (أيقونة + اسم) بنفس شكل `.topbar .brand`
  بالظبط (كلاس CSS جديد `.module-brand`) بدل العنوان النصي العادي اللي
  كان موجود.
- **زرار رفع مستقل لكل تصنيف مستند** في مودال ملف المريض
  (`render-patients.js → renderPatientModal`): بدل ما الرفع يبقى بس من
  شاشة "رفع ملف" المنفصلة بدروب داون فئة، دلوقتي كل تصنيف (تحقيق
  شخصية/تأمين/أشعة/تحاليل/وصفة طبية/رسم مخ/أخرى) عنده زرار "+ رفع" جنب
  اسمه مباشرة — بيفتح منتقي ملف (`accept="image/*,application/pdf"`)
  ويرفع فوراً لنفس الفئة من غير ما يحتاج يختار الفئة من دروب داون
  منفصل. فئة "أخرى" بتظهر خانة وصف قبل ما تفتح منتقي الملف. زرار
  التنزيل لكل ملف كان موجود بالفعل من قبل (لكل ملف على حدة، مش لكل
  تصنيف) وفضل زي ما هو.
- **`Auth.changePassword(newPassword)`** جديدة في `auth.js` (عبر
  `supabase.auth.updateUser({password})`) + مودال "تغيير كلمة السر"
  جديد في `app.js` (`openChangePasswordModal`) — بيتحقق من طول كلمة
  السر (٦ حروف على الأقل) وتطابق التأكيد، وبيستخدم `toast` للنجاح/الخطأ
  زي باقي الداشبورد (مش `alert()`).
- بصمة الكاش اترفعت لـ `?v=17` في `index.html`.

**إصلاح — أرشيف المرضى/الليدز كانوا متكررين في مكانين (٢٠٢٦-٠٨-١٨ — `?v=18`)**:

- بعد إضافة القائمة المنسدلة الموحّدة (`?v=17`)، اتلاحظ إن "أرشيف المرضى"
  و"إدارة الليدز والتواصل" و"لوحة التحكم" كانوا لسه ظاهرين **كمان** كتابات
  عادية في شريط التابات الرئيسي (`#tabs-bar`) وفي قائمة الموبايل القديمة
  (`#mobile-menu`) — يعني متكررين في مكانين بدل ما يبانوا بس جوه القائمة
  المنسدلة زي ما المستخدم طلب بالظبط.
- **الحل**: `tabButtonsHtml()` في `app.js → renderShell` بقت تستخدم
  `mainSuiteTabs` (تابات SSMPD الأساسية بس) بدل كل التابات — فبقى شريط
  التابات العادي وقائمة الموبايل القديمة يعرضوا ٦ تابات بس، والتلاتة
  المتبقيين (أرشيف المرضى/الليدز/لوحة التحكم) موجودين حصرياً جوه القائمة
  المنسدلة الجديدة (`#user-dropdown`). التنقّل والصلاحيات (`canSeeTab`)
  فضلوا زي ما هم من غير أي تغيير.
- اختبارات `test/smoke.js` اتحدّثت لتعكس الوضع الجديد (٦ تابات مش ٩ في
  الشريط/قائمة الموبايل، + تأكيد وجود التلاتة تابات جوه القائمة المنسدلة
  بس).
- بصمة الكاش اترفعت لـ `?v=18` في `index.html`.

**المراحل الجاية**: اختبار شامل إضافي على الموبايل → مراجعة نهائية مع
المستخدم لموديولي أرشيف المرضى وإدارة الليدز → التحليلات المستقبلية لو
اتطلبت.

**ملاحظة مش باگ**: لو ظهرت رسالة "جسر Google Drive لسه مش متظبط" رغم إن
`config.js` فيه رابط حقيقي — هي غالباً تبويب/صفحة قديمة لسه فاتحة من قبل
آخر تحديث (`?v=N` بيتغيّر مع كل رفعة عشان يجبر المتصفح يجيب نسخة جديدة،
بس لازم عمل ريفريش فعلي للصفحة مش بس فتح مودال جديد فيها). لو حصلت تاني
بعد ريفريش كامل (Ctrl+Shift+R)، يبقى لازم نتأكد من قيمة `driveBridge.webAppUrl`
فعلياً في `config.js` على GitHub.

**تم أيضاً — تعدد الأدوار لكل مستخدم + استكمال سير عمل الحجز (حالتين جداد) +
التحويل التلقائي لأرشيف المرضى + إحصائيات دخل الفواتير (٢٠٢٦-٠٨-١٨ — `?v=19`)**:

- **تعدد الأدوار (طلب المستخدم)**: أي مستخدم ممكن يبقى له أكتر من دور دلوقتي
  (مثال المستخدم بالظبط: خدمة عملاء + استقبال، أو خدمة عملاء + إدارة محتوى).
  القرار المعماري: `admins.role` فضل "الرول الأساسي" (بيتحكم في التاب
  الافتراضي وبادچ الدور فوق)، وأي أدوار إضافية بتتسجل في جدول جديد
  `admin_extra_roles` (admin_id + role). دالة جديدة `has_role(role)` في
  `setup.sql` (+ نسخة `has_role_for(admin_id, role)` للـ Edge Functions اللي
  شغالة بمفتاح service_role) بترجع true لو الرول ده أساسي أو إضافي — كل
  سياسات RLS اللي كانت بتفحص `my_role() = 'x'`/`in (...)` اتحدّثت تستخدمها
  بدل المقارنة المباشرة (`content_items`, `weekly_social_metrics`,
  `ad_campaigns`, `leads` وكل الجداول التابعة له)، وكذلك
  `guard_content_transition()` (بقى بيفحص "اتحاد" كل أدوار المستخدم بدل رول
  واحد بس)، و`is_super()`/`can_manage_all_content()`/`has_archive_access()`/
  `can_access_leads()`/`has_archive_review_access()` اتعادوا تعريفهم يشملوا
  الأدوار الإضافية برضه. **لازم يتشغّل `setup.sql` تاني في Supabase SQL
  Editor** عشان الجدول والدوال الجديدة تتفعّل (آمن للتشغيل أكتر من مرة زي
  العادة). كل الـ ١٢ Edge Function بتاعة موديولي الليدز/أرشيف المرضى
  اتحدّثت تجيب الأدوار الإضافية وتفحصها (مفيش تحقق صلاحيات عندها بيعدّي من
  RLS أصلاً، لأنها بتشتغل بمفتاح service_role اللي بيتخطى RLS بالكامل — فالتحقق
  فيها لازم يبقى يدوي في كود الدالة نفسها).
  الواجهة: `roles.js` بقى فيه `rolesOf`/`hasRole`/`hasAnyRole`/`labelAll`
  (بتقبل صف admin كامل فيه `.roles` أو رول كـ string لوحده للتوافق مع كود/
  اختبارات قديمة)، و`canSeeTab`/`defaultTab`/`isSuperAdmin`/`canApprove`/
  `canDesign`/`canCreateContent` كلها بقت بتفحص الأدوار مجتمعة.
  `auth.js → loadCurrentAdmin` بقى بيجيب الأدوار الإضافية من
  `admin_extra_roles` بعد الدخول ويحطها في `currentAdmin.roles`.
  لوحة "المستخدمون والصلاحيات" (`render-admin.js`) بقى فيها عمود جديد
  "أدوار إضافية" — چك بوكس لكل الأدوار ما عدا الأساسي، بيضيف/يشيل من
  `admin_extra_roles` مباشرة (`db.js → addAdminExtraRole`/
  `removeAdminExtraRole`، مقصورة على `is_super()` في RLS).
- **حالتين جداد لسير عمل الحجز في موديول الليدز (طلب المستخدم بالظبط)**:
  بعد "تم الحجز" (booked — خدمة العملاء بتقفل بيها بعد ما تتفق مع العميل
  وتاخد رقم/مرجع الحجز)، الليد بيروح للاستقبال يكمّل الحجز فعلياً على
  سيستم المركز → حالة جديدة **"تم الحجز على سيستم المركز"**
  (`booked_on_system`)، وبعد ما الخدمة تتم فعلياً الاستقبال بيقفل بحالة
  **"تم إجراء الخدمة"** (`service_done`). القيم دي اتضافت لـ
  `leads.current_status` check constraint في `setup.sql` ولـ
  `VALID_STATUSES` في `leads-update-status`. "مغلق" (`CLOSED_STATUSES`)
  بقى بيقصد `service_done`/`rejected`/`no_response`/`invalid_number` بس
  (مش `booked` زي الأول) — عشان `booked`/`booked_on_system` لسه "شغالين"
  محتاجين متابعة استقبال، اتحدّث في كل مكان بيستخدمه (`leads-list`,
  `leads-create`, `leads-bulk-create`, `render-leads.js`).
- **التحويل التلقائي لأرشيف المرضى + رفع الفاتورة "على ملف المريض" (طلب
  المستخدم بالظبط، بدون دوبليكيشن)**: `lead-invoice-upload` Edge Function
  اتعادت كتابتها بالكامل — رفع فاتورة الخدمة (متاح من حالة "تم الحجز" لحد
  "تم إجراء الخدمة") هو نفسه اللي بيعمل كل الآتي في عملية واحدة:
  ١) يدوّر على مريض موجود بمطابقة `phone_normalized` (نفس منطق تطبيع
  الهاتف المستخدم في `patients-create`/`leads-create`)، ولو مفيش بينشئ
  مريض جديد — كده تحويل الليد لملف مريض بيحصل تلقائي من غير أي خطوة يدوية
  منفصلة، ومفيش دوبليكيشن لأن المطابقة بالتليفون بتحصل قبل الإنشاء.
  ٢) يرفع ملف الفاتورة **على فولدر المريض نفسه على Drive** (فئة جديدة
  `invoice` في `patient_files.category`، فولدر فرعي "Invoices" — مش فولدر
  عام منفصل زي قبل كده) عشان تبقى فعلاً "على ملفه" زي ما طلب المستخدم، مع
  تسجيلها في `patient_files` (بتدخل نفس طابور المراجعة `pending` القياسي
  زي أي مستند تاني) و`archive_access_log`.
  ٣) يسجّل الفاتورة في `lead_invoices` (عمود جديد `patient_file_id` بيربطها
  بملف المريض المقابل) — ده أساس إحصائيات الدخل تحت.
  ٤) يربط `leads.patient_id`/`patient_type` بالمريض، ويقفل الليد تلقائياً
  بحالة "تم إجراء الخدمة" (عن طريق `rpc_update_lead` الموجود بالفعل، عشان
  سجل تغيير الحالة يوثّق مين عمل العملية صح).
  مودال تفاصيل الليد (`render-leads.js`) بقى بيوضح "✓ اتحوّل لملف مريض في
  الأرشيف" بعد ما التحويل يحصل، وقسم الفاتورة بقى ظاهر من حالة "تم الحجز"
  لحد "تم إجراء الخدمة" (مش "تم الحجز" بس زي الأول).
- **إحصائيات دخل الفواتير حسب الموظف المسؤول عن إتمام الحجز (طلب المستخدم
  بالظبط)**: داشبورد الإدارة في موديول الليدز (`renderDashboardScreen`)
  بقى فيه مؤشر "إجمالي دخل الفواتير" + جدول "الدخل حسب الموظف المسؤول عن
  إتمام الحجز" (مجمّع حسب `leads.booked_by` — نفس عمود "مين أنهى الحجز"
  الموجود بالفعل من دفعة سابقة). المصدر: `leads-list` Edge Function (وضع
  `stats=1`) بقى بيرجّع كمان `total_income`/`invoices_count`/
  `income_by_employee` (array فيها اسم الموظف وعدد الفواتير وإجمالي
  المبلغ لكل واحد، مرتبة تنازلياً).
- **تصحيح باگ مكتشف أثناء الشغل** (مش من ضمن الطلب، لكن اتصحّح لأنه في
  نفس السطر اللي كنت بعدّله أصلاً): `patient-files-list` Edge Function
  كانت بتفحص `caller.has_archive_review_access` بس الـ `select` بتاع صف
  الأدمن ماكانش شامل العمود ده أصلاً — فكانت بترجع `undefined` دايماً،
  ويبقى صاحب صلاحية "مراجعة الأرشيف" (`has_archive_review_access=true`)
  مايقدرش يشوف طابور المراجعة إلا لو سوبر أدمن. اتصلح بإضافة العمود لـ
  `select`.
- بصمة الكاش اترفعت لـ `?v=19` في `index.html`.

**فجوات معروفة من الدفعة دي (تبسيطات مقصودة عشان الوقت، مش بق)**:
- توزيع الليدز التلقائي في `leads-create` وقائمة "الموظف اللي أنهى الحجز"
  في `leads-list` (`list_employees`) بيشملوا موظفي خدمة العملاء بالرول
  الأساسي أو الإضافي في الأول، لكن `list_employees` نفسها (فلتر أرشيف
  الليدز) لسه بتفلتر بالرول الأساسي بس — موظف عنده `customer_service` كرول
  إضافي مش هيظهر في دروب داون الفلتر ده (هيفضل يشتغل عادي، بس مش هيبان في
  الفلتر). كذلك دروب داون اختيار "مصمم" في `render-review.js` (لإسناد مادة
  لمصمم) لسه بيفلتر بالرول الأساسي `designer` بس. لو احتجتوا الاتنين
  يشملوا الأدوار الإضافية كمان، سهل يتعمل بنفس نمط `leads-create`.

**تم أيضاً — تصحيح شعار الهيدر + فصل بصري بين موديولات النظام (طلب المستخدم
بالظبط، ٢٠٢٦-٠٨-١٨ — `?v=20`)**:

- **شعار الهيدر بقى ديناميكي**: اسم الشعار في أعلى الصفحة (`.topbar .brand`)
  كان نص ثابت "SSMPD" (ماله معنى فعلي لموديولات زي أرشيف المرضى/الليدز اللي
  مالهاش علاقة بإنتاج محتوى سوشيال ميديا أصلاً). بقى دلوقتي "مركز عيادات
  سونو التخصصية" + اسم السكشن المفتوح حالياً (مثال: "مركز عيادات سونو
  التخصصية | أرشيف المرضى")، وبيتحدّث لحظياً مع كل `switchTab()`. اللوجو
  نفسه (`<img src="assets/img/mark.svg">`) فضل زي ما هو من غير أي تغيير —
  اتغيّر النص جنبه بس (`assets/js/app.js`).
- **فصل بصري بين موديولات النظام (طلب المستخدم بالظبط: "لما افتح حاجة
  ماتظهرليش هيدر حاجتين مختلفتين")**: شريط تابات السويت الرئيسي (`#tabs-bar`
  — الملخص العام/إنتاج المحتوى/إدارة المحتوى/شاشة التصميم/النشر/الأرشيف)
  كان فاضل ظاهر فوق حتى لما تكون فاتح موديول منفصل (أرشيف المرضى/الليدز/
  لوحة التحكم)، فيبان هيدرين فوق بعض. بقى دلوقتي `switchTab()` بيخفي
  `#tabs-bar` (وقائمة الموبايل المقابلة `.mm-tabs`) تماماً لما التاب الحالي
  يبقى واحد من الموديولات المنفصلة الثلاثة، ويرجّعهم لما نرجع لتاب من
  السويت الرئيسي.
- **إزالة الهيدر المكرر جوه الموديولين**: بعد ما بقى شعار الهيدر الخارجي
  نفسه بيعرض اسم الموديول المفتوح، بقى الهيدر الداخلي `.module-brand`
  (لوجو + اسم) اللي كان فوق كل من `render-patients.js` و`render-leads.js`
  زيادة ومكرر (تلات مرات: هيدر خارجي + هيدر داخلي + اسم السكشن) — اتشال من
  الملفين الاتنين، والصفحة بقت شاشة واحدة نضيفة تحت الهيدر الديناميكي بس.
  (كلاس `.module-brand` في `styles.css` سايبينه من غير استخدام حالياً، مش
  بيأثر على حاجة، ممكن يتشال لاحقاً لو حبينا).
- بصمة الكاش اترفعت لـ `?v=20` في `index.html`.

**إصلاح — القائمة المنسدلة كانت مختفية تماماً على الموبايل/التابلت + تصحيح
تعارض رفع متزامن بين شاتين (٢٠٢٦-٠٨-٢٠ — `?v=21`)**:

- المستخدم بلّغ إنه على الموبايل مش لاقي أي طريقة يوصل بيها لأرشيف المرضى/إدارة
  الليدز/لوحة التحكم، وإن الشكل رجع للتصميم القديم (بادچ الدور + الاسم + خروج
  بس، من غير أيقونة ☰).
- **السبب الحقيقي**: قاعدة CSS قديمة `@media (max-width:860px){ .topbar
  .who{display:none;} #tabs-bar{display:none;} .menu-toggle{display:flex;} }`
  كانت بتخفي `.who` بالكامل على الشاشات الصغيرة — وأيقونة القائمة المنسدلة
  الجديدة (`.user-menu-icon`) كانت **جوه** `.who`، فبتختفي معاه تماماً.
- **الحل**: `.topbar .who{display:none}` اتلغت، وبدالها بس بادچ الدور/الاسم/زرار
  الخروج بيتخفوا على الشاشات الصغيرة (موجودين أصلاً جوه القائمة المنسدلة نفسها)
  وأيقونة `.user-menu-icon` بتفضل ظاهرة على كل الشاشات. شريط تابات SSMPD
  (`#tabs-bar`) بقى بيفضل ظاهر ويعمل سكرول أفقي على الموبايل بدل ما يختفي.
  ملحوظة: إخفاء/إظهار `#tabs-bar` عن طريق JS في `switchTab()` (لموديولات
  أرشيف المرضى/الليدز/لوحة التحكم، من دفعة `?v=20`) لسه شغّال عادي وبيغلب
  إعداد الـ CSS ده — مفيش تعارض.
- **⚠️ حادثة تعارض رفع متزامن (لازم تتوثق)**: أثناء عمل الإصلاح ده، حصل رفع
  لملفي `index.html` و`CLAUDE.md` من غير عمل `git fetch` قبلها أول — فده
  مسح مؤقتاً توثيق دفعتين سابقتين (`?v=19` تعدد الأدوار + سير عمل الحجز،
  و`?v=20` شعار الهيدر الديناميكي وفصل الموديولات) من `CLAUDE.md`، ورجّع
  بصمة الكاش في `index.html` من `?v=20` لـ `?v=19` (تعارض ترقيم — نفس رقم
  `v=19` كان مستخدم قبل كده في دفعة تانية خالص). **اتصحّح فوراً**: التوثيق
  المفقود اترجّع كامل هنا، وبصمة الكاش اترفعت لـ `?v=21` (رقم جديد بعد
  الاتنين) عشان نضمن كل المتصفحات تجيب النسخة الصح من كل الملفات. **درس
  للمستقبل**: أي شات بيشتغل على المشروع ده لازم يعمل `git fetch origin
  main` + يقارن قبل أي `git add`/رفع لو فيه احتمال شات تاني اشتغل على نفس
  الريبو في الفترة دي.
- بصمة الكاش اترفعت لـ `?v=21` في `index.html`.

**فجوات معروفة / تبسيطات مقصودة بسبب الوقت**:

- ربط API فيسبوك/انستجرام لمؤشرات السوشيال ميديا **مش متوفر** — الإدخال
  يدوي أسبوعي من مسؤول الاعتماد. لو احتجتوا ربط تلقائي لاحقاً، يحتاج
  Meta Graph API + موافقة المركز على صلاحيات الصفحة.
- الحارس في `guard_content_transition()` لحالة `needs_revision` بيفرّق
  بين "رفض قبل التصميم" و"رفض بعد التصميم" بالاعتماد على وجود
  `assigned_designer` من عدمه — تبسيط منطقي معقول لكن مش حارس متكامل
  ١٠٠٪ لكل الحالات الاستثنائية.
- تحديث الشاشات اللحظي (Realtime) بسيط: أي تغيير على `content_items`
  بيعيد رسم التاب الحالي بالكامل (مش diff دقيق) — كافي لحجم الاستخدام
  المتوقع، ولو الفريق كبر ممكن يحتاج تحسين لاحقاً.
- لا يوجد اختبار حقيقي على متصفح فعلي — jsdom فقط + تأكيد يدوي على
  الرابط اللايف بعد كل نشر.
- استيراد CSV لمؤشرات السوشيال ميديا **تخمين بالكلمات المفتاحية** مش
  مطابقة دقيقة لصيغة رسمية من Meta/Google — لازم مراجعة الأرقام يدوياً
  قبل الحفظ في كل مرة، خصوصاً لو الملف مختلف شكله عن المتوقع.
- صلاحية تعديل حالة الكومنت (`pending`/`done`) مفتوحة لأي موظف نشط
  (`active admins update comments` في `setup.sql`) — تبسيط مقصود لأن
  الفريق صغير؛ لو احتجتوا تقييدها لصاحب الكومنت بس أو مسؤول الاعتماد،
  يحتاج تعديل السياسة.

**إصلاح — بطء داشبورد الليدز/أرشيف المرضى + باگ "الباسورد غلط" (٢٠٢٦-٠٨-٢٢ — `?v=22`)**:

المستخدم بلّغ عن ٣ مشاكل: (أ) بطء ملحوظ في تحميل موديولي الليدز وأرشيف
المرضى، (ب) بعض الموظفين بياخدوا "البريد أو كلمة السر غلط" مع إنهم متأكدين
إن البيانات صح، (ج) "تم استنفاذ محاولات التأكيد بالإيميل" لما أكتر من موظف
بيفعّل حسابه في نفس الوقت.

- **(أ) البطء**: كل الـ ١٢ Edge Function بتاعة الموديولين (`getCallerAdmin`
  في كل واحدة منهم) كانت بتعمل استعلامين متتاليين منفصلين — واحد على
  `admins` وواحد تاني على `admin_extra_roles` — **قبل** ما تنفّذ الاستعلام
  الحقيقي المطلوب، فوق `auth.getUser()` نفسها. يعني ٣ رحلات شبكة متتالية
  إجبارية قبل أي بيانات فعلية، تتكرر مع كل نداء (كل تاب فرعي/زرار). الحل:
  استعلام `admins` بقى بيجيب `admin_extra_roles(role)` مضمومة معاه (join
  عن طريق PostgREST embedding، معتمد على الـ FK الموجود أصلاً) في نفس
  الاستدعاء بدل نداء منفصل — رحلة شبكة واحدة بدل اتنين لكل نداء. الملفات
  المتأثرة: `patients-create`, `patient-files-upload`, `patient-files-list`,
  `patient-files-download`, `patient-files-delete`, `patient-files-review`,
  `leads-create`, `leads-list`, `leads-attempt`, `leads-update-status`,
  `leads-bulk-create`, `lead-invoice-upload` (كل ملفات `supabase/functions/*`
  اللي فيها `getCallerAdmin`). **لازم تُنشر يدوياً على Supabase** (كل دالة
  عن طريق Monaco editor في الداشبورد — نفس طريقة نشر Edge Functions
  الموصوفة فوق) — مش كفاية إنها اترفعت على GitHub بس.
- **(ب) "الباسورد غلط"**: حقل الباسورد في شاشة الدخول (`app.js`) ما كانش
  بيتعمله `.trim()` زي الإيميل بالظبط، ومفيش `autocomplete`/
  `autocapitalize="off"`/`autocorrect="off"` على حقلَي الإيميل والباسورد.
  الموظفين بيستلموا/ينسخوا الباسورد كتير من رسايل واتساب/ماسنجر — مسافة
  أو سطر جديد مخفي في الآخر، أو auto-capitalize من كيبورد الموبايل، بيخلي
  القيمة المُرسلة مختلفة عن الأصل فعلياً فيرفضها Supabase برسالة عامة
  "Invalid login credentials". الحل: `.trim()` على الباسورد (شاشة الدخول
  ومودال "تغيير كلمة السر" الاتنين)، + `autocomplete="current-password"`/
  `"new-password"`/`"username"` + `autocapitalize="off" autocorrect="off"
  spellcheck="false"` على كل حقول الإيميل/الباسورد.
- **(ج) استنفاذ محاولات التأكيد**: مش باگ في الكود — رسالة خام من Supabase
  بترجع زي ما هي (مفيش لها ترجمة في `translateAuthError`). السبب: المشروع
  مستخدم خدمة الإيميل الافتراضية المجانية بتاعة Supabase (مفيش SMTP خاص
  متظبط في `config.js` ولا في الـ secrets) — وده محدود جداً (تقريباً
  ٢-٤ إيميلات/ساعة **على مستوى المشروع كله**، مش لكل مستخدم)، فلما أكتر من
  موظف يسجّلوا/يطلبوا تأكيد في نفس الوقت الحد بيتصرف بسرعة. **الحل محتاج
  قرار المستخدم** (اختيار مزوّد SMTP وعمل حساب فيه) — لسه معلّق، مش جزء من
  تعديلات الكود دي.
- بصمة الكاش اترفعت لـ `?v=22` في `index.html`.

**تصحيح حرج — باگ FK غامض كسر موديولي الليدز/أرشيف المرضى بالكامل (٢٠٢٦-٠٨-٢٢ — `?v=22` hotfix)**:

- بعد نشر تحسين `?v=22` فوق (تجميع استعلام `admins`+`admin_extra_roles` في نداء
  واحد بدل اتنين)، المستخدم بلّغ برسالة "غير مصرح — سجّل دخولك تاني" ظاهرة
  فجأة في كل مكان بموديولي الليدز/أرشيف المرضى. السبب: `admin_extra_roles`
  عندها **فورين كي اتنين** لجدول `admins` (`admin_id` و`added_by`)، فـ
  `admins.select("id, role, ..., admin_extra_roles(role)")` بقى غامض
  (PostgREST مش عارف يفهم مين الـ FK المقصود) وبيرمي خطأ — وكود الدالة
  ماكانش بيفحص `error` من الاستعلام (بس `if (!row)`)، فـ`row` كان بيرجع
  `null` لأي مستخدم، و`getCallerAdmin()` بترجع `null` لأي حد، فكل نداء
  Edge Function كان بيرجع 401. الإصلاح: تحديد الـ FK صراحة —
  `admin_extra_roles!admin_id(role)` بدل `admin_extra_roles(role)` — في
  الـ ١٢ Edge Function كلهم. **درس**: أي embed عن طريق PostgREST لجدول
  فيه أكتر من FK لنفس الجدول التاني لازم يحدد اسم الـ FK صراحة، وأي كود
  بيتعامل مع نتيجة استعلام Supabase لازم يفحص `error` مش بس يفترض إن
  `null` يعني "مفيش صف".

**تم أيضاً — دور جديد "طبيب سونو" (معاينة أرشيف المرضى فقط) (٢٠٢٦-٠٨-٢٢ — `?v=23`)**:

- طلب المستخدم: دور جديد يتبعتله ملفات المرضى من الأرشيف يتصفحها بس — من
  غير رفع أو حذف أو مراجعة. نفس نمط `has_archive_access`/
  `has_archive_review_access` الموجودين بالفعل (صلاحية منفصلة عن الرول
  الأساسي، تتفعّل من تشيك بوكس في لوحة "المستخدمون والصلاحيات"):
  - رول جديد `sono_doctor` ("طبيب سونو") — بادچ/تسمية بس في الواجهة، مفيش
    له أي صلاحية تلقائية بحد ذاته (زي `nursing` بالظبط)
  - عمود جديد `admins.has_archive_view_only` — لما يتفعّل، بيدّي وصول
    قراءة بس لتاب "أرشيف المرضى" (`roles.js → canSeeTab`) وشاشتي الداشبورد
    والتصفح، **من غير** تاب "رفع ملف" (`canUpload()` في
    `render-patients.js` بيفحص `has_archive_access` بس، مش الفلاج الجديد
    — فمحفوظ زي ما هو تلقائياً) وزرار "حذف" لكل ملف (كان ظاهر لأي حد قبل
    كده بغض النظر عن الصلاحية — اتصحّح كمان يتفحص `canUpload()`)
  - `setup.sql`: دالة جديدة `has_archive_view_only()` + توسعة سياسات
    القراءة بس (مش الكتابة) على `patients`/`patient_files`/
    `archive_access_log` لتشملها، + إضافة `sono_doctor` لقيود
    `admins_role_check`/`admin_extra_roles_role_check` — **لازم يتشغّل
    `setup.sql` تاني في Supabase SQL Editor**
  - Edge Functions المتأثرة (قراءة بس): `patient-files-list` و
    `patient-files-download` — بقوا يقبلوا `has_archive_view_only` زي
    `has_archive_access`. باقي الدوال (`patient-files-upload`/`delete`/
    `review`/`patients-create`) **من غير تغيير عمداً** — لسه مقصورة على
    `has_archive_access` فقط، عشان صاحب "معاينة فقط" ميقدرش يرفع/يحذف/يعدّل
    أي حاجة
  - `defaultTab()`: `sono_doctor` بيهبط مباشرة على تاب "أرشيف المرضى" (زي
    `nursing` بالظبط) بدل ما يهبط على تاب مش موجود أصلاً في `TAB_ACCESS`
    (نفس فخ الباگ اللي اتصحّح فوق لـ"مسؤول المحتوى" — أي رول جديد لازم
    مسار واضح في `defaultTab`/`canSeeTab` وإلا هيهبط على شاشة مش مسموح
    له يشوفها)
- بصمة الكاش اترفعت لـ `?v=23` في `index.html`.
- **تحديث**: تم تشغيل `setup.sql` (القسم الجديد بس، منفصل عن باقي الملف
  بسبب مشكلة `admins_role_check` القديمة — شوف الملاحظة تحت) وعمل Deploy
  لـ`patient-files-list`/`patient-files-download` بنجاح. الفيتشر شغالة
  بالكامل على الإنتاج.

**ملاحظة مهمة — تشغيل `setup.sql` كامل ممكن يفشل بـ`admins_role_check` violation**:
عند محاولة تشغيل ملف `setup.sql` كامل من أوله لآخره، فشل بـ
`ERROR: 23514: check constraint "admins_role_check" ... is violated by
some row` — رغم إن فحص مباشر لجدول `admins` أثبت إن مفيش أي صف فيه قيمة
`role` غير متوقعة. السبب المرجّح: تعريف مبكر وأضيق للـ constraint (قبل
إضافة أدوار زي `reception`/`nursing`) بيتنفّذ الأول في نفس الترانزاكشن
قبل ما يوصل لتعريفه الأوسع الأخير، فبيتصادم مع صفوف موجودة بالفعل من
أدوار اتضافت في وقت سابق. **الحل العملي**: بدل تشغيل الملف كامل، شغّل
بس الـ SQL الخاص بالتعديل الجديد (القسم المطلوب فقط) في SQL Editor.

**درس تاني اتكرر هنا — Monaco/SQL Editor مش دايماً بيلتقط `ctrl+Return`**:
لما بتحدّث نص الكويري بـ`model.setValue()` من الكونسول وبعدين تدوس
`ctrl+Return`، أحياناً الكويري القديمة اللي كانت شغالة قبلها هي اللي
بترن (نتيجة قديمة). الطريقة الموثوقة: اضغط بالماوس فعلياً على زرار
"Run" في الواجهة بعد التأكد من إن النص المعروض في المحرر هو الجديد
(بلقطة شاشة)، مش الاعتماد على اختصار الكيبورد لوحده.

## تغيير المصمم المسؤول عن مادة (`?v=24` — ٢٠٢٦-٠٨-٢٣)

- طلب المستخدم بعد بلاغ "المادة معتمدة للتصميم مش ظاهرة للمصمم" — اتضح
  إن المشكلة مش باگ، المصمم اللي كان بيدوّر كان شخص مختلف عن اللي
  اتسندت له المادة أصلاً (تم اختيار مصمم غلط وقت الاعتماد الأولي). طلب
  المستخدم إضافة أوبشن لتغيير/نقل المادة لمصمم تاني بعد ما تكون اتسندت
  خلاص.
- `render-review.js` (شاشة "إدارة المحتوى"، مودال المراجعة): لو فيه
  `assigned_designer` على المادة، بيظهر سلكت "المصمم المسؤول" + زرار
  "تغيير المصمم" — متاح في أي مرحلة بعد `initial_approval` (`in_design`،
  `needs_revision`، `final_approval`، وحتى بعدها لو حصل غلط)، مش مقصور
  على لحظة الاعتماد الأولي بس زي ما كان قبل كده.
  - عند التغيير: `updateContentItem` بيحدّث `assigned_designer` فقط،
    والمرحلة (`stage`) **بتفضل زي ما هي** — مفيش داعي لتعديل
    `guard_content_transition()` في `setup.sql` لأن القاعدة
    `old.stage = new.stage` مسموحة بالفعل لكل الأدوار (مسؤول الاعتماد/
    المدير العام/السوبر أدمن).
  - لو المادة لسه `in_design`/`needs_revision`، بيتصفّر
    `design_received_at` كمان عشان المصمم الجديد يشوفها "في انتظار
    الاستلام" مش "تم الاستلام" غلط (الحالة دي كانت خاصة بالمصمم القديم).
  - في `confirm()` قبل التنفيذ يوضّح اسم المصمم القديم والجديد، وبيتسجّل
    Activity Log بالتغيير.
  - مفيش تعديل في RLS/SQL — الصلاحية الموجودة أصلاً كافية.
- بصمة الكاش اترفعت لـ `?v=24` في `index.html`.

## "طبيب سونو" بقى نظام إحالة، مش أرشيف كامل للمعاينة (`?v=25` — ٢٠٢٦-٠٨-٢٣)

- طلب المستخدم بعد تجربة الرول: مش عايز "طبيب سونو" يشوف أرشيف المرضى كله
  حتى معاينة — عايزه يشوف بس الحالات اللي التمريض حوّلها له، ولما يخلص
  الكشف يعمل "تم الكشف" فتختفي من عنده. هيدّي تعديل معماري على قرار
  `?v=23` (كان `has_archive_view_only` = وصول قراءة لكل الأرشيف).
- `setup.sql` قسم ١٥ (بعد قسم ١٣ بتاع الرول الأساسي):
  - جدول جديد `patient_doctor_assignments` (patient_id, doctor_id,
    assigned_by, status pending/done, assigned_at, completed_at) — RLS:
    الدكتور يشوف/يعدّل بس اللي ليه، والتمريض/صاحب أرشيف كامل يقدر
    يعمل insert (بشرط الدكتور المُسنَد فعلاً `has_archive_view_only=true`
    و`active`) ويشوف/يعدّل الكل.
  - دالة `is_assigned_doctor_for_patient(patient_id)` — بتتفحص من RLS
    على `patients`/`patient_files` (سياستي القراءة اتعدّلوا: بدل
    `has_archive_view_only()` البلانكت، بقى `is_assigned_doctor_for_patient(id)`
    — يعني الدكتور بس يقدر يقرا صف المريض/الملف لو فيه إحالة `pending` ليه).
  - دالة `list_active_sono_doctors()` — عشان شاشة الإحالة تجيب أسماء
    الأطباء النشطين من غير ما نفتح جدول `admins` كله لكل الموظفين (سياسة
    `admins` الأساسية بتدّي كل حد صفه بس أو لو سوبر أدمن يشوف الكل —
    فضلت زي ما هي عمداً، الدالة دي بديل أضيق وأأمن).
  - `archive_access_log` رجع لصلاحيتي الأرشيف الكاملتين بس (مش محتاج
    الدكتور يشوف سجل الوصول).
- **مهم**: أرشيف المرضى/الملفات (`patients`/`patient_files`) بيتقرا من
  التطبيق دايماً عن طريق Edge Functions (`patient-files-list`/`download`)
  اللي بتستخدم service role وبتتخطى RLS بالكامل — فالـ RLS فوق دفاع إضافي
  بس، مش نقطة التحقق الحقيقية. التحقق الفعلي اتضاف يدوي جوه الـ Edge
  Functions نفسها:
  - `patient-files-list/index.ts`: متغير `doctorOnly` (عنده
    `has_archive_view_only` بس، من غير أرشيف كامل ولا مراجعة). لو
    `patient_id` متبعت، بيتفحص وجود إحالة `pending` قبل ما يرجّع الملفات.
    الداشبورد العام (`stats=1`) بقى 403 للدكتور (مش محتاجه). قائمة
    المرضى من غير `patient_id`/`review_status` بقت — للدكتور بس — بترجع
    المرضى المحالين له `pending` فقط (join على `patient_doctor_assignments`)
    بدل بحث/تصفح كل الأرشيف.
  - `patient-files-download/index.ts`: اتعمل reorder — بيجيب صف الملف
    الأول (`patient_id` بتاعه) قبل ما يقرر `allowed`، عشان يقدر يتحقق من
    الإحالة لنفس المريض ده تحديداً قبل السماح بالتنزيل.
- `db.js`: دوال جديدة `listActiveSonoDoctors` (rpc)، `assignPatientToDoctor`،
  `listMyDoctorAssignments`، `completeDoctorAssignment` — الإحالة/الإتمام
  بيعدّوا مباشرة على الجدول (مش عن طريق Edge Function) لأنهم مش محتاجين
  Google Drive، فبيتبعوا نفس نمط `content_items`/`comments` في الملف ده.
- `render-patients.js`:
  - `isDoctorOnly()`: `has_archive_view_only` بس من غير أرشيف كامل/مراجعة
    → `render()` بيحوّل بالكامل لشاشة مختلفة `renderDoctorQueue` (طابور
    "الحالات المحالة لك" — فتح ملفات + زرار "تم الكشف")، بدل التابات
    الأربعة العادية (داشبورد/رفع/مراجعة/تصفح).
  - `canAssignDoctor()`: تمريض، أو صاحب أرشيف كامل، أو سوبر أدمن — بيظهر
    زرار "تحويل لطبيب سونو" جنب كل مريض في شاشة "تصفح وفلترة"، بيفتح
    مودال صغير لاختيار الطبيب (من `list_active_sono_doctors()`) والتأكيد.
  - "تم الكشف" بيعمل `completeDoctorAssignment` (status → done) وبيعيد
    رسم الطابور فوراً فتختفي الحالة من عند الدكتور.
- بصمة الكاش اترفعت لـ `?v=25` في `index.html`.

## إصلاح "خطأ إرسال إيميل التأكيد" + إصلاح RLS "تحويل لطبيب سونو" (٢٠٢٦-٠٨-٢٣)

- بلاغ المستخدم: شاشة التسجيل بتظهر "Error sending confirmation email".
  السبب: خدمة الإيميل الافتراضية/المجانية في Supabase محدودة جداً (٢-٤
  إيميلات/ساعة على مستوى المشروع كله — نفس القيد الموثّق من `v22`). تم
  إيقاف "Confirm email" في إعدادات Supabase Auth (Sign In/Providers) كحل
  مؤقت. المستخدم ذكر إنهم بيستخدموا Resend فعلاً — لسه معلّق قرار ربط
  Resend كـ SMTP مخصص لو حابين يرجّعوا تأكيد الإيميل بدل إيقافه بالكامل.
- بلاغ المستخدم: "new row violates row-level security policy for table
  patient_doctor_assignments" عند "تحويل لطبيب سونو". السبب الحقيقي (بعد
  محاولة إصلاح أولى غير كافية بإضافة `has_archive_access()` الناقصة من
  سياسة "pda insert"): الشرط `exists (select 1 from admins d where
  d.id = doctor_id ...)` جوه سياسة `patient_doctor_assignments` كان
  بيتفحص تحت RLS الخاصة بالمستخدم الحالي على جدول `admins` نفسه — واللي
  بتدّي كل حد صفه بس (مش صف الدكتور المطلوب التحقق منه) — فكان بيرجع
  false دايماً لأي حد مش super_admin، ويبوّظ الميزة بالكامل لكل التمريض/
  أصحاب الأرشيف. **درس معماري**: أي subquery جوه سياسة RLS بيقرا من جدول
  تاني محمي بـ RLS بيتنفذ تحت صلاحية المستخدم المستدعي على الجدول التاني
  ده — مش bypass تلقائي. الحل: دالة `SECURITY DEFINER` جديدة
  `is_active_sono_doctor(p_doctor_id uuid)` بتتخطى RLS بتاعة `admins`
  عمداً وبأمان للفحص الضيق ده بس (نفس نمط `my_role()`/`has_archive_access()`/
  `my_admin_id()` الموجود بالفعل). سياسة "pda insert" اتعدّلت تنادي
  الدالة دي بدل الـ subquery المباشر.

## بيانات شخصية إضافية للمريض — مرحلة ١ من طلبات الفريق (`?v=26` — ٢٠٢٦-٠٨-٢٣)

- المستخدم بعت ٥ صور واتساب فيها تعديلات مطلوبة من الفريق + ٤ ملفات docx
  نموذج فايلنج المركز (كبار/اطفال/تجميل/كماوي)، وطلب تقسيمها مراحل
  وتنفيذها مرحلة مرحلة (بأقل استهلاك كوتا ممكن). المرحلة الأولى المُنفَّذة
  هنا: إضافة بيانات شخصية أساسية على ملف المريض (سن/نوع/رقم طبي/تاريخ
  آخر زيارة) — تمهيداً لتقسيم الملف لاحقاً لجزء بيانات شخصية وجزء بيانات
  طبية (المراحل الجاية، حسب نماذج الـ docx الأربعة).
- `setup.sql` قسم ١٦: أعمدة جديدة على `patients` — `gender` (`male`/`female`
  بس، بـ check constraint)، `age` (int)، `medical_record_no` (text)،
  `last_visit_date` (date). كل الأعمدة nullable — مفيش migration للبيانات
  القديمة.
- `patients-create/index.ts`: بياخد `gender`/`age`/`medical_record_no`
  اختياريين من الـ body، ويحط `last_visit_date` = تاريخ اليوم تلقائياً عند
  الإنشاء. الـ select الراجع اتوسّع ليشملهم.
- `patient-files-list/index.ts`: الـ `select` بتاع جلب مريض واحد
  (`patient_id`) وقائمة التصفح/البحث اتوسّعوا ليشملوا الأعمدة الجديدة.
  - **هوتفيكس نشر**: أول نسخة استخدمت template literal (backtick) في فلتر
    البحث (`query.or(\`full_name.ilike...\`)`) وده فشّل نشر الدالة على
    Supabase بخطأ "Expected unicode escape" من الـ bundler. اتصلّح
    باستبدال الـ template literal بـ string concatenation عادي — نفس
    المنطق، وبيشتغل مضمون مع bundler دينو.
- `db.js`: دالة جديدة `updatePatientRecord(id, patch)` — بتعدّل مباشرة
  على جدول `patients` عن طريق الـ Supabase JS client (مش Edge Function)،
  لأن سياسة RLS الموجودة أصلاً (`"archive or leads update patients"` —
  `has_archive_access() or can_access_leads()`) كافية لهذا المسار.
- `render-patients.js`:
  - `openNewPatientModal`: حقول جديدة (السن/النوع/الرقم الطبي) في نموذج
    "مريض جديد".
  - `renderBrowseScreen`: أعمدة جديدة في جدول التصفح (السن/النوع/الرقم
    الطبي/آخر زيارة) + زرار "تعديل البيانات" جديد لكل صف.
  - دالة جديدة `openEditPatientModal(patient, onSaved)`: مودال تعديل
    بيانات المريض (اسم/هاتف/سن/نوع/رقم طبي/تاريخ آخر زيارة) — بينادي
    `updatePatientRecord`.
  - `renderPatientModal`: قسم جديد "البيانات الشخصية" في أعلى مودال
    المريض (قبل أقسام الملفات) بيعرض الحقول الجديدة + زرار "تعديل" بيفتح
    نفس مودال التعديل.
- بصمة الكاش اترفعت لـ `?v=26` في `index.html`.
- **درس تسجيل توثيق**: إصلاح `is_active_sono_doctor()` كان اتطبّق مباشرة
  على قاعدة البيانات الحية الأسبوع اللي فات من غير ما يترفع فوراً لملف
  `setup.sql` المحلي/GitHub — تم رصد الفجوة دي وتصحيحها هنا (قسم ١٥ في
  `setup.sql` بقى مطابق تماماً للنسخة الحية). **الدرس**: أي إصلاح SQL
  بيتعمل مباشرة في محرر Supabase لازم يترفع لملف `setup.sql` المحلي
  بنفس الدفعة، مش يتأجل — تجنباً لـ drift بين الكود المحلي والقاعدة الحية.

## تقسيم ملف المريض: بيانات طبية منفصلة (`?v=27` — مرحلة ٢ — ٢٠٢٦-٠٨-٢٣)

- استكمالاً لمرحلة ١، ومبني على تحليل ٤ نماذج فايلنج ورقية بعتها المستخدم
  (كبار/اطفال/تجميل/كماوي): كل نموذج فيه قسمين — (أ) بيانات طبية شبه ثابتة
  عن حالة المريض (طبيب معالج/تخصص/علامات حيوية حالية/أمراض مزمنة/عمليات
  جراحية/تاريخ مرضي بالعائلة)، و(ب) سجل زيارات متكرر (تاريخ/شكوى/خطة
  علاجية/متابعة). المرحلة دي بتفصل الاتنين دول عن `patients` (اللي فيه
  بس البيانات الشخصية من مرحلة ١) في جدولين جدد.
- `setup.sql` قسم ١٧:
  - `patient_medical_profile` (صف واحد لكل مريض، `patient_id` primary
    key): طبيب معالج، تخصص، علامات حيوية (ضغط/سكر/وزن/نبض/أكسجين) كأعمدة
    نصية، + ٣ أعمدة `jsonb` (`chronic_conditions`/`surgeries`/
    `family_history`) بدل أعمدة منفصلة لكل حالة — عشان القوائم دي تختلف
    حسب التخصص (مثلاً نموذج الكماوي غير الاطفال) من غير ما نحتاج نعدّل
    الـ schema في كل مرة.
  - `patient_visits` (صف لكل زيارة): تاريخ/رقم زيارة/شكوى/خطة علاج
    (أدوية/أشعة/تحاليل/توصيات)/تاريخ متابعة + علامات حيوية خاصة بالزيارة
    (بيتسجّلوا في نموذج الكماوي مع كل متابعة، عكس النماذج التانية اللي
    العلامات الحيوية فيها ثابتة في الملف الرئيسي).
  - RLS: القراءة بنفس دائرة قراءة `patients` (أرشيف/مراجعة/leads/الدكتور
    المحال له عن طريق `is_assigned_doctor_for_patient`) — لازم الدكتور
    يقدر يشوف البيانات الطبية أصلاً، ده الهدف من الإحالة. الكتابة
    (insert/update/delete) لأصحاب أرشيف كامل أو سوبر أدمن بس — الدكتور
    "معاينة فقط" حتى لو الواجهة اتنسي فيها تقييد.
  - مفيش Edge Function جديدة: الجدولين اتوصلوا مباشرة بـ Supabase JS
    client (زي `content_items`/`comments`)، لأن RLS كافية ومفيش تكامل مع
    Google Drive مطلوب هنا.
- `db.js`: `getPatientMedicalProfile`، `savePatientMedicalProfile` (upsert
  بمفتاح `patient_id`)، `listPatientVisits`، `addPatientVisit`،
  `updatePatientVisit`، `deletePatientVisit`.
- `render-patients.js`:
  - `openPatientModal`/`reloadModal`: بيجيبوا `patient_files` +
    `patient_medical_profile` + `patient_visits` بالتوازي (`Promise.all`)
    قبل الرسم.
  - `renderPatientModal`: قسم جديد "البيانات الطبية" بعد "البيانات
    الشخصية" مباشرة وقبل أقسام الملفات — بيعرض بيانات الملف الطبي +
    جدول سجل الزيارات. زرار "تعديل" وزرار "+ زيارة جديدة" وزرار حذف
    الزيارة بيظهروا بس لو `canUpload()` (نفس شرط رفع الملفات) — الدكتور
    (`isDoctorOnly()`) يشوف القسم كامل من غير أي زرار تعديل/إضافة/حذف،
    وده بيلبّي جزء من مطلب "authorization... يشوف بس من غير أي تعديل"
    (مرحلة ٥ الجاية هتراجع باقي التفاصيل).
  - دالة جديدة `openEditMedicalProfileModal(patient, profile, onSaved)`:
    مودال بيانات طبية كامل — حقول نصية للطبيب/التخصص/العلامات الحيوية،
    + تشيك ليست ثابتة لـ ٧ أمراض مزمنة (تدخين/ضغط/سكر/غدة درقية/كلى/
    أورام/حساسية أدوية) كل واحدة معاها checkbox + حقل دواء اختياري، +
    قوائم ديناميكية (إضافة/حذف صف) للعمليات الجراحية والتاريخ المرضي
    بالعائلة. الحفظ بينادي `savePatientMedicalProfile` (upsert).
  - دالة جديدة `openAddVisitModal(patient, onSaved)`: مودال إضافة زيارة
    (تاريخ/رقم/شكوى/علامات حيوية/خطة علاج/متابعة) — بينادي `addPatientVisit`.
- بصمة الكاش اترفعت لـ `?v=27` في `index.html`.
- **الأقسام الطبية اللي لسه معلّقة لمراحل جاية** (حسب الفروق بين النماذج
  الأربعة اللي اتحللت): تخصيص شكل "خطة العلاج" لنموذج الكماوي (بيتكرر لكل
  متابعة بدل جدول واحد ثابت — الجدول الحالي `patient_visits` بيغطي الحالة
  دي بالفعل عن طريق كل صف زيارة، فمفيش تعديل schema مطلوب، بس ممكن تحتاج
  الواجهة تمييز بصري حسب التخصص لاحقاً)، وربط "التخصص" في `specialty` هنا
  بحقل "الخدمة" في شاشة الليدز (مرحلة ٦).

## شاشة الاستقبال: صندوق "ملف جديد" (`?v=28` — مرحلة ٣ — ٢٠٢٦-٠٨-٢٣)

- استكمالاً لطلبات فريق الاستقبال (سكرين شوتس واتساب): تبويب "رفع ملف"
  القديم في شاشة المرضى اتحول لـ "الاستقبال" — صندوق "ملف جديد" واحد بعد
  اختيار/إنشاء المريض، فيه ٤ أزرار إجراء بدل ما كان بس فورم رفع مباشر:
  - **تعديل بيانات**: بينادي `openEditPatientModal` الموجودة بالفعل من
    مرحلة ١ (بيانات شخصية).
  - **رفع مستندات**: بيظهر/يخفي (toggle) نفس فورم الرفع بالفئة اللي كان
    شغال قبل كده — اتحول من عرض مباشر لدالة منفصلة `renderUploadForm`
    قابلة لإعادة الاستخدام بعد كل رفع ناجح.
  - **إرسال للتمريض**: زرار جديد بينادي `sendPatientToNursing(id)` في
    `db.js` — بيسجّل `sent_to_nursing_at = now()` على صف المريض. مفيش
    جدول workflow منفصل اتعمل، لأن التمريض أصلاً عندها صلاحية أرشيف
    كاملة، والخطوة اللي بعد كده (تحويل لطبيب سونو) موجودة بالفعل من
    مرحلة ١/قسم ١٥ (`patient_doctor_assignments`) — العمود ده مجرد علامة/
    سجل تدقيق إن الاستقبال خلّص وسلّم الملف، مش state machine كامل.
    الصندوق بيعرض "✓ اتبعت للتمريض (تاريخ)" لو العمود متسجل.
  - **حذف**: زرار جديد بيأكد (`window.confirm`) ثم بينادي
    `deletePatientRecord(id)` في `db.js`. لو فيه `leads` مرتبطة بالمريض،
    الحذف هيفشل بخطأ foreign key (عمداً — `leads.patient_id` من غير
    `on delete cascade` عشان نحافظ على تاريخ الليدز)، والواجهة بتلاقط
    الخطأ ده تحديداً (`e.code === "23503"` أو رسالة تحتوي "foreign key"/
    "violates") وتعرض رسالة واضحة بدل خطأ Postgres خام.
- `setup.sql` قسم ١٨:
  - عمود `patients.sent_to_nursing_at timestamptz` (nullable).
  - سياسة حذف جديدة على `patients` (ماكانتش موجودة قبل كده) بنفس دائرة
    الكتابة/التعديل الحالية: `has_archive_access() or can_manage_all_content()`.
    الجداول التانية المرتبطة (`patient_files`/`patient_medical_profile`/
    `patient_visits`/`patient_doctor_assignments`) كلها `on delete cascade`
    فبتتشال تلقائي مع المريض.
- **اختيار الطبيب المعالج/التخصص عند الإنشاء**: مودال "+ مريض جديد"
  (`openNewPatientModal`) اتضاف له حقلين اختياريين (طبيب معالج/تخصص) —
  نص حر (مش قائمة أطباء سونو المسجلين) لأن الطبيب وقت الاستقبال ممكن
  يكون طبيب خارجي/عام مش بالضرورة مستخدم مسجل في النظام. لو اتملوا،
  بيتسجّلوا في `patient_medical_profile` عن طريق نداء غير-حاجب
  (non-blocking) لـ `savePatientMedicalProfile` بعد نجاح إنشاء المريض —
  لو فشل النداء ده، الفلو مكمّل عادي (البيانات الطبية اختيارية عند
  الإنشاء، ممكن تتعمل لاحقاً من المودال الطبي).
- `patient-files-list` (Edge Function): إضافة `sent_to_nursing_at` لعمودي
  الـ `select` (جلب مريض واحد + قائمة/بحث المرضى) عشان الواجهة تقدر تعرض
  حالة "اتبعت للتمريض".
- بصمة الكاش اترفعت لـ `?v=28` في `index.html`.

## تصنيفات رفع المستندات: علاج طبيعي + تقرير طبي (`?v=29` — مرحلة ٤ — ٢٠٢٦-٠٨-٢٣)

- طلب الفريق (سكرين شوتس واتساب) كان فيه قائمة تصنيفات لرفع المستندات:
  تحاليل/أشعة/روشتة/علاج طبيعي/تقرير طبي. بمراجعة `CATEGORIES` الحالية في
  `render-patients.js` تبيّن إن تحاليل/أشعة/روشتة موجودين بالفعل بأسماء
  مختلفة قليلاً (`lab_result`/`radiology`/`prescription`) — فاتضاف بس
  التصنيفين الناقصين: `physical_therapy` ("علاج طبيعي") و
  `medical_report` ("تقرير طبي")، من غير حذف أي تصنيف قديم (`id_document`/
  `insurance`/`eeg`/`other`) لأنهم لسه مستخدمين في أرشفة الملف الورقي.
- `render-patients.js`: إضافة العنصرين لمصفوفة `CATEGORIES` — بما إن كل
  شاشات الفئة (فورم الرفع، شاشة المراجعة، شاشة التصفح) بتقرأ من نفس
  المصفوفة عن طريق دالة واحدة `categoryLabel()`، مفيش تعديل تاني مطلوب.
- `setup.sql` قسم ١٩: تحديث `patient_files_category_check` (CHECK
  constraint) عشان يقبل القيمتين الجديدتين — القيد ده لازم يتحدّث كل مرة
  تصنيف جديد يتضاف، وإلا الرفع هيفشل بخطأ قاعدة بيانات.
- بصمة الكاش اترفعت لـ `?v=29` في `index.html`.

## مراجعة صلاحيات شاشة الدكتور "معاينة فقط" (`?v=30` — مرحلة ٥ — ٢٠٢٦-٠٨-٢٣)

- المطلوب: التأكد إن "طبيب سونو" (`isDoctorOnly()` — عنده `has_archive_view_only`
  بس، من غير أرشيف كامل أو مراجعة) يقدر **يشوف بس** الحالات المحالة له، من
  غير أي إمكانية تعديل/رفع/حذف — حتى لو حد نسي تقييد في الواجهة.
- **النتيجة: التقييد كان بالفعل مطبّق بالكامل من مرحلتين ١ و٢** — المراجعة دي
  تحقق (audit) وليست إصلاح جوهري:
  - الواجهة: `render()` بتحوّل الدكتور لشاشة منفصلة `renderDoctorQueue`
    (طابور الإحالات بس) قبل ما تعرض أي تبويب تاني — الدكتور مايوصلش
    أصلاً لتبويبات الاستقبال/المراجعة/التصفح ولا لصندوق "ملف جديد"
    (مرحلة ٣). كل أزرار التعديل/الرفع/الحذف/إضافة زيارة في مودال المريض
    (`renderPatientModal`) متلفوفة بشرط واحد `canUp = canUpload()` اللي
    بيرجع false للدكتور دايماً.
  - القاعدة (الحماية الحقيقية — مش مجرد إخفاء واجهة): كل الـ RLS policies
    والـ Edge Functions المرتبطة بالكتابة (`patient_files` رفع/حذف،
    `patient_medical_profile`/`patient_visits` إضافة/تعديل/حذف، تعديل
    بيانات `patients`) مقصورة على `has_archive_access() أو
    can_manage_all_content()` — الدكتور مايقدرش ينفذها حتى بنداء API
    مباشر لو تخطى الواجهة. تنزيل الملفات (`patient-files-download`)
    مسموح للدكتور بس لمريض عنده إحالة `pending` فعلية ليه بالاسم (اتفحص
    ضد `patient_doctor_assignments`)، و"تم الكشف" (`pda update`) مقصور
    على تحديث إحالته هو بس (`doctor_id = my_admin_id()`).
  - **إصلاح صغير واحد اتلقى أثناء المراجعة**: زرار "تعديل البيانات" في
    شاشة "تصفح وفلترة" (`renderBrowseScreen`) كان ظاهر لأي حد يوصل
    للشاشة دي من غير شرط `canUpload()` — مش خطر أمني (RLS كانت هتمنع
    التعديل فعلياً) بس تجربة استخدام مربكة لأي مستخدم عنده مراجعة بس
    (`has_archive_review_access`) من غير أرشيف كامل. اتلف الزرار بنفس
    شرط `canUp` المستخدم في باقي الشاشة.
- بصمة الكاش اترفعت لـ `?v=30` في `index.html`.

## توسيع قائمة "الخدمة" باهتمامات الليدز (`?v=31` — مرحلة ٦ — ٢٠٢٦-٠٨-٢٣)

- طلب الفريق (سكرين شوتس واتساب) كان فيه قائمة خدمات لاهتمام الليد: كشف/
  أشعة/معمل/علاج طبيعي/أسنان/تخاطب/نفسية/ليزر تجميل/تمريض تحويل/طوارئ.
  بمراجعة `SERVICE_LABELS` الحالية في `render-leads.js`: كشف/أشعة/تمريض/
  علاج طبيعي موجودين بالفعل، و"معمل" اتعتبر مرادف لـ"تحاليل" (`lab`)
  الموجودة أصلاً فما اتضافش تكرار. اتضافت الخدمات الناقصة الواضحة بس:
  `dental` ("أسنان")، `speech_therapy` ("تخاطب")، `psychiatry` ("نفسية")،
  `cosmetic_laser` ("ليزر تجميل")، `emergency` ("طوارئ").
  - **ملحوظة**: عبارة "تمريض تحويل" في الطلب الأصلي ماتضافتش كخدمة منفصلة
    لغموضها (تمريض موجودة بالفعل، و"تحويل" مش واضح لو معناها خدمة تانية
    ولا وصف لعملية تحويل المريض لطبيب سونو الموجودة أصلاً من مرحلة ١).
    لو المقصود خدمة منفصلة، محتاجة توضيح من الفريق.
- `render-leads.js`: إضافة الخدمات الجديدة لمصفوفة `SERVICE_LABELS` — نفس
  نمط `CATEGORIES` في مرحلة ٤، مصدر واحد بيغذّي القائمة المنسدلة وعرض
  التسمية في كل مكان (مفيش تعديل تاني مطلوب).
- `setup.sql`: تحديث `leads_interested_service_check` (CHECK constraint
  على `leads.interested_service`) عشان يقبل الخدمات الجديدة. الـ Edge
  Functions (`leads-create`/`leads-bulk-create`/`leads-list`) بتمرر القيمة
  كنص عادي من غير whitelist منفصلة، فمفيش تعديل مطلوب فيهم.
- بصمة الكاش اترفعت لـ `?v=31` في `index.html`.

## داشبورد الإدارة: فلتر تاريخ + دخل حسب القسم + تصنيف عضوي/إعلان + عرض ملف قبل
## الاعتماد (`?v=33` — مرحلة ٧ — ٢٠٢٦-٠٨-٢٣)

- طلب الفريق (سكرين شوتس واتساب) كان فيه تعديلين على داشبورد الإدارة في
  موديول الليدز: (أ) فلتر تاريخ على أقسام الدخل، و(ب) تمييز مصدر اهتمام
  الليد (وصل من نفسه/عضوي مقابل جه من إعلان مدفوع) بشكل واضح مع إمكانية
  فلترته بالتاريخ.
- **عمود جديد `leads.acquisition_type`** (`organic`/`ad`، اختياري) —
  مختلف عن `source` (قناة التواصل واتساب/ماسنجر) الموجود أصلاً. اتضاف
  دروب داون اختياري له في فورم استقبال ليد جديد، وعرض له في مودال تفاصيل
  الليد لو متسجل.
- **`leads-list` (وضع `stats=1`) اتحدّث بالكامل**: بقى يقبل فلتر تاريخ
  اختياري (`from`/`to`) بيتطبّق على `created_at` لليدز (لتوزيع الحالة
  وتصنيف عضوي/إعلان) وعلى `uploaded_at` للفواتير (لأقسام الدخل) — أما
  إجماليات "مفتوحة/مغلقة/تم الحجز" فبتفضل عمداً غير متأثرة بالفلتر لأنها
  بتعبّر عن الوضع الحالي اللحظي مش سجل تاريخي. رجّع كمان `by_acquisition_type`
  (عضوي/إعلان/غير معروف) و**"الدخل حسب القسم"** (`income_by_department`) —
  ده بيعيد استخدام عمود `leads.requested_department` الموجود أصلاً (مفيش
  عمود/جدول قسم جديد اتعمل) مجمّع من نفس بيانات الفواتير المستخدمة أصلاً
  في "الدخل حسب الموظف" (نفس نمط، تجميع تاني بس).
- `render-leads.js → renderDashboardScreen` اتعاد كتابتها بالكامل: فلتر
  تاريخ (من/إلى + تطبيق/مسح)، سكشن KPI لتصنيف عضوي/إعلان بالنسب المئوية،
  وجدول جديد "الدخل حسب القسم" — جنب سكشنات الدخل/الحالة/الموظف الموجودة
  من قبل.
- **إضافة أثناء الشغل بناءً على طلب مباشر من المستخدم**: في شاشة "مراجعة
  قبل الاعتماد" بأرشيف المرضى (`render-patients.js → renderReviewScreen`)،
  اتضاف زرار "عرض" جنب "اعتماد"/"رفض" لكل ملف قيد المراجعة — بيفتح الملف
  في تاب جديد للمعاينة قبل اتخاذ قرار الاعتماد، بدل ما يكون لازم يعتمد
  الملف الأول عشان يقدر يشوفه.
- **تصحيح صلاحية اتكشف أثناء إضافة زرار "عرض"**: `patient-files-download`
  Edge Function ماكانتش بتفحص `has_archive_review_access` خالص (كان
  `select` استعلام الأدمن مش شامل العمود ده أصلاً) — يعني صاحب "مراجعة
  الأرشيف" بس (من غير أرشيف كامل) كان يقدر يشوف طابور المراجعة لكن
  مايقدرش يفتح/ينزّل أي ملف فيه، فيبقى زرار "عرض" هيفشل له تحديداً. اتصلح
  بإضافة العمود للـ `select` وإضافته لشرط `allowed`/`canReviewOrFull` —
  نفس نمط الأخطاء المشابهة اللي اتصلحت قبل كده في المشروع ده (عمود ناقص
  من `select` بيخلي الصلاحية ترجع `undefined` دايماً).
- بصمة الكاش اترفعت لـ `?v=33` في `index.html`.

## زرار "عرض" و"تعديل" لسجل الزيارات في ملف المريض (`?v=34` — ٢٠٢٦-٠٨-٢٣)

- طلب المستخدم: في جدول "سجل الزيارات" بمودال المريض كان فيه زرار "حذف"
  بس لكل زيارة — اتضاف زرار "عرض" (مودال قراءة فقط بكل تفاصيل الزيارة)
  وزرار "تعديل" (نفس فورم "زيارة جديدة" لكن بيتعبّى بالقيم الحالية
  وبيحفظ بـ`updatePatientVisit` الموجودة بالفعل من مرحلة ٢ بدل إنشاء
  زيارة جديدة). الزرارين ظاهرين بنفس شرط زرار الحذف (`canEditMedical`).
- `render-patients.js`: `openAddVisitModal` اتحولت لـ`openVisitFormModal(patient,
  existingVisit, onSaved)` — لو `existingVisit` متبعت بيبقى وضع تعديل
  (الحقول متعبّاة، والحفظ بينادي `updatePatientVisit`)، ولو `null` بيبقى
  نفس سلوك الإضافة القديم بالظبط. دالة جديدة `openViewVisitModal(v)`
  لعرض التفاصيل قراءة فقط. مفيش تعديل SQL/Edge Function مطلوب — الدوال
  اللي بتُستخدم موجودة بالفعل في `db.js`.
- بصمة الكاش اترفعت لـ `?v=34` في `index.html`.

## تغيير كلمة السر: من عند السوبر أدمن لأي مستخدم + "نسيت كلمة السر" في شاشة
## الدخول (`?v=35` — ٢٠٢٦-٠٨-٢٣)

- طلب المستخدم بعد بلاغ "البريد أو كلمة السر غلط" لمستخدم بعد إيقاف/إعادة
  تفعيل حسابه (اتأكد وقتها إن الحساب سليم ومش موقوف — على الأغلب كلمة سر
  منسية): (أ) اوبشن عند السوبر أدمن يغيّر كلمة سر أي مستخدم مباشرة من لوحة
  "المستخدمون والصلاحيات"، و(ب) اوبشن "نسيت كلمة السر" في شاشة الدخول نفسها
  (كان موجود قبل كده تغيير كلمة السر بس من قائمة المستخدم بعد الدخول —
  مفيش فايدة لمستخدم مقفول برا أصلاً).
- **Edge Function جديدة `admin-set-password`**: مقصورة على `super_admin`
  بس (نفس دائرة الوصول للوحة "المستخدمون والصلاحيات" أصلاً) — بتاخد
  `admin_id`/`new_password`، تدوّر على `user_id` المرتبط بيه في جدول
  `admins`، وتستخدم `auth.admin.updateUserById()` (بمفتاح service_role،
  ده أساساً السبب في احتياجها Edge Function منفصلة — تغيير باسورد مستخدم
  تاني من غير معرفة القديم مش متاح من عميل عادي). لو المستخدم لسه معلّق
  (`user_id` فاضي، ماعملش حساب بعد)، بترجع خطأ واضح.
- `render-admin.js`: زرار "تغيير كلمة السر" جديد لكل صف (متعطّل لو المستخدم
  لسه معلّق) بجانب "إيقاف/تفعيل"/"حذف" — بيفتح مودال بسيط (باسورد جديد +
  تأكيد) وبينادي `db.js → adminSetUserPassword`.
- **"نسيت كلمة السر" في شاشة الدخول** (`app.js`): لينك جديد تحت زرار "دخول"
  يفتح مودال إيميل بس، وبينادي `Auth.resetPasswordForEmail()` الجديدة
  (`auth.js` — wrapper حول `client.auth.resetPasswordForEmail`، بترجع
  المستخدم لنفس رابط الداشبورد). الرسالة اللي بتظهر للمستخدم عامة وثابتة
  (سواء الإيميل مسجل فعلاً أو لأ، أو حصل خطأ) — نفس مبدأ الأمان المعتاد
  (منكشفش إيميلات مسجلة عندنا من عدمها). لما المستخدم يضغط على اللينك اللي
  بيوصله بالإيميل، بيرجع لصفحة الداشبورد بجلسة مؤقتة تلقائية من Supabase،
  وده بيطلق حدث `PASSWORD_RECOVERY` — اتضاف مستمع له في `app.js` (عند بدء
  التشغيل، قبل حتى التأكد من وجود جلسة عادية) بيفتح مودال "تغيير كلمة
  السر" الموجود بالفعل من قبل تلقائياً، فالمستخدم يكتب باسورد جديد ويحفظ.
- **ملاحظة مهمة (فجوة معروفة، مش باگ)**: مسار "نسيت كلمة السر" عن طريق
  الإيميل بيعتمد على نفس خدمة الإيميل المجانية المحدودة الموثّقة في `v22`
  (٢-٤ إيميلات/ساعة على مستوى المشروع كله) — ممكن ميوصلش فوراً لو فيه
  طلبات تانية في نفس الساعة. **الحل الأسرع والمضمون فعلياً هو اوبشن
  السوبر أدمن** (تغيير مباشر بدون إيميل خالص) — ده المسار المتوقع
  للاستخدام الفعلي لحد ما يتحل موضوع SMTP المخصص (لسه معلّق من `v22`).
- بصمة الكاش اترفعت لـ `?v=35` في `index.html`.

## داشبورد الإدارة في موديول الليدز بقى ظاهر لخدمة العملاء (`?v=36` — ٢٠٢٦-٠٨-٢٦)

- طلب المستخدم: فتح تاب "داشبورد الإدارة" (شاشة المؤشرات/الإحصائيات) جوه
  موديول "إدارة الليدز" لموظف خدمة العملاء كمان — كان قبل كده مقصور على
  `general_manager`/`super_admin` بس (`visibleSubScreens()` في
  `render-leads.js`)، عشان الموظف يقدر يشوف مؤشرات أدائه.
- `render-leads.js → visibleSubScreens()`: تاب `dashboard` بقى ظاهر لو
  `isManager() || isCS()` بدل `isManager()` بس. باقي التابات (الاستقبال/
  رفع إكسيل) فضلت زي ما هي (مقصورة على الاستقبال/المدير). الداشبورد نفسه
  من غير تعديل — بيعرض كل المؤشرات (دخل/حالة/حسب الموظف/حسب القسم) بدون
  فلترة لموظف بعينه، فخدمة العملاء هتشوف بيانات كل الفريق مش بس بتاعتها.
- بصمة الكاش اترفعت لـ `?v=36` في `index.html`.

## إصلاح: أسماء "بواسطة/صاحب كومنت/مصمم مسؤول" كانت فاضية لأي حد غير السوبر
## أدمن (`?v=37` — ٢٠٢٦-٠٨-٢٦)

- بلاغ المستخدم: "نشر بواسطة" وحقول أسماء تانية شغالة كويس بس في يوزره
  (سوبر أدمن)، وفاضية/غلط عند باقي المستخدمين.
- **السبب**: `render-production.js`/`render-review.js`/`render-design.js`/
  `render-publish.js`/`render-archive.js` كانوا بيستخدموا
  `db.js → listAdmins()` (بتعمل `select("*") from admins` مباشر تحت RLS)
  بس لعرض أسماء (صاحب المادة/المصمم/ناشر المنشور/صاحب كومنت) — مش لإدارة
  مستخدمين فعلية. سياسة RLS الأصلية على `admins` ("read own or pending")
  بترجع لأي مستخدم عادي **صف نفسه بس**، والسوبر أدمن بس (`super reads all`)
  بيشوف كل الصفوف — فخريطة `adminsById` كانت فاضية عملياً لغير السوبر أدمن،
  فكل اسم غير اسمه هو بيطلع "—"/"مستخدم محذوف".
- **الحل**: دالة جديدة `list_admins_basic()` (`SECURITY DEFINER`، زي نمط
  `list_active_sono_doctors()` بالظبط) بترجع بس `id/name/role/active` (من
  غير إيميل/user_id) لكل الموظفين — متاحة لأي مستخدم مسجّل دخول. الشاشات
  الخمس اتحوّلت تستخدم `db.js → listAdminsBasic()` (RPC) بدل `listAdmins()`
  المباشرة. `render-admin.js` (لوحة "المستخدمون والصلاحيات" الفعلية، محتاجة
  بيانات كاملة + `has_archive_access`/الأدوار الإضافية) فضلت على
  `listAdmins()` القديمة زي ما هي عمداً — مقصورة على السوبر أدمن أصلاً.
- `setup.sql` قسم ٢٠: تعريف الدالة الجديدة — **لازم تتشغّل في Supabase SQL
  Editor** قبل ما الإصلاح يشتغل فعلياً (نفس الجزء بس، مش الملف كامل، بسبب
  مشكلة `admins_role_check` الموثّقة قبل كده).
- بصمة الكاش اترفعت لـ `?v=37` في `index.html`.

## إصلاحات لغوية + حذف الليدز بصلاحية + ملاحظات نصية لكل ملف أرشيف (`?v=39` —
## ٢٠٢٦-٠٨-٢٩)

طلب الفريق ٣ حاجات (سكرين شوتس واتساب + طلب مباشر):

- **تصحيح لغوي**: "عضوي" (ترجمة حرفية لـ organic كانت غامضة للفريق) اتغيّرت
  لـ"طبيعي" في `ACQUISITION_LABELS` — المصطلح التسويقي العربي الصحيح لمصدر
  اهتمام "وصل من نفسه" مقابل "إعلان". مصدر استلام الليد (`SOURCE_LABELS`)
  اتضاف له "تليفون" و"عيادة" (بدل "كلينك" العامية) بجانب واتساب/ماسنجر —
  `setup.sql` (`leads_source_check`) و`leads-create` Edge Function اتحدّثوا
  يقبلوا القيمتين الجديدتين.
- **حذف الليد بصلاحية**: عمود جديد `admins.can_delete_leads` (نفس نمط
  `has_archive_access`) — متاح تلقائياً للسوبر أدمن، وقابل للتفعيل لأي
  مستخدم تاني من لوحة "المستخدمون والصلاحيات". دالة `can_delete_leads()`
  + سياسة `"leads delete"` جديدة في `setup.sql` (لم تكن موجودة أصلاً —
  ما كانش فيه حذف لليدز خالص قبل كده). زرار "حذف الليد" في مودال تفاصيل
  الليد (`render-leads.js`) بيظهر بس لمين عنده الصلاحية، بتأكيد بضغطة
  تانية على نفس الزرار (بدل `confirm()` المتصفح، اتساقاً مع قاعدة `v13`).
- **ملاحظات نصية لكل ملف في أرشيف المرضى**: خانة "وصف نوع الملف" اللي
  كانت مقصورة على فئة "أخرى" بس اتعمّمت لكل الفئات (بما فيها "وصفة طبية")
  كـ"ملاحظات/تفاصيل الملف (اختياري)" — إجبارية لفئة "أخرى" بس زي الأول.
  مفيش تعديل SQL/Edge Function مطلوب: عمود `patient_files.other_description`
  والـ`select` في `patient-files-list` كانوا أصلاً عامّين (مش مقصورين على
  فئة بعينها)، الفرونت‌إند بس كان بيقصر الحقل على "أخرى".
- بصمة الكاش اترفعت لـ `?v=39` في `index.html`.

## "عملاء ناقصين بيانات" + أرشيف ومقارنة تقارير المؤشرات/الإعلانات (`?v=38` —
## ٢٠٢٦-٠٨-٢٦)

- طلب الفريق (٣ طلبات): (أ) صفوف الإكسيل الناقصة اسم/تليفون وقت رفع ملف
  الليدز الجماعي كانت بتتجاهل تماماً — عايزين شاشة منفصلة تجمعها عشان
  الموظف يكلم العميل ويكمل الناقص. (ب) أرشيف لتقارير مؤشرات السوشيال ميديا
  الأسبوعية + مقارنة بين فترتين + إجماليات. (ج) نفس الحاجة بالظبط لتقارير
  حملات إعلانات Meta Ads (كانت بتتمسح وتتستبدل بالكامل كل استيراد).

**(أ) عملاء ناقصين بيانات**:
- `leads-bulk-create`: صف فيه اسم أو تليفون بس (مش الاتنين مع بعض) بقى
  بيتحفظ كـ `leads` بحالة جديدة `missing_data` (بدل التجاهل الكامل قبل
  كده) — لسه بيتسند لأقل موظف خدمة عملاء عنده ليدز مفتوحة زي أي ليد عادي،
  عشان RLS تسمح له يشوفه/يكمله. لو الاتنين ناقصين مع بعض (مفيش أي حاجة
  تحدد العميل بيها) لسه بيتخطى زي الأول بالظبط. الرد بقى فيه
  `missing_data_count`/`missing_data` بجانب `created`/`skipped`.
- `setup.sql` قسم ٢١: `missing_data` اتضافت لـ `leads_current_status_check`،
  وعمود جديد `leads.missing_data_completed_at` (بيتسجل وقت استكمال
  البيانات — بيفضل فارق حتى بعد ما الحالة تتغيّر لـ`new`، أساس حساب مؤشر
  "تم استكمال بياناتهم").
- Edge Function جديدة `leads-complete-missing-data`: بتاخد `lead_id`/
  `customer_name`/`phone`، تتحقق إن الليد فعلاً `missing_data` وإن المستخدم
  مسموح له (المُسند له، أو استقبال/مدير/سوبر أدمن)، تطبّع التليفون وتتأكد
  مفيش تكرار، تربط بمريض موجود لو اتلاقى بنفس منطق `leads-create`، وتحوّل
  الحالة لـ`new` + تسجّل `missing_data_completed_at`.
- `leads-list`: بقت ترجّع `missing_data_completed_at` في الـ`select`، وتقبل
  فلتر جديد `completed_missing_data=1` (لحساب مؤشر "تم الاستكمال").
- تاب جديد "عملاء ناقصين بيانات" في موديول الليدز (`render-leads.js`) —
  ظاهر لخدمة العملاء/الاستقبال/المدير. فيه ٤ مؤشرات (عملاء جدد آخر ٢٤
  ساعة، إجمالي العملاء، تم استكمال بياناتهم، لسه محتاجين استكمال) + جدول
  قائمة الانتظار مع زرار "استكمال بيانات" بيفتح مودال بسيط (اسم + تليفون)
  بينادي `db.js → completeMissingDataLead`.
- ملخص نتيجة رفع الإكسيل الجماعي بقى يوضّح عدد الصفوف اللي اتحوّلت لقائمة
  الانتظار دي، مش بس المُنشأة/المتخطاة.

**(ب+ج) أرشيف ومقارنة التقارير — شاشة "الملخص العام" (`render-summary.js`)**:
- **مؤشرات السوشيال ميديا الأسبوعية**: كانت أصلاً بيانات تراكمية (صف لكل
  أسبوع، `weekly_social_metrics.week_start` فريد) — مفيش تغيير SQL مطلوب،
  بس الواجهة كانت بتجيب آخر أسبوعين بس. بقت تجيب أرشيف كامل (لحد ٥٠٠
  أسبوع) وتعرض: سكشن جديد "أرشيف مؤشرات السوشيال ميديا" فيه إجماليات كل
  الأسابيع (وصول/متابعين جدد تراكمي + متوسط نسبة تفاعل)، مقارنة تفاعلية
  بين أي أسبوعين (دروب داون لكل واحد)، وجدول كل الأسابيع كاملاً.
- **تقارير حملات Meta Ads**: كانت تصميمياً "استبدال كامل" كل استيراد (مفيش
  أرشيف أصلاً) — اتحوّلت لأرشيف تراكمي: عمود جديد `ad_campaigns.report_batch_id`
  (uuid واحد مشترك لكل صفوف نفس الاستيراد، بيتولّد في المتصفح وقت الحفظ)،
  و`clearAdCampaigns()` بطل يتنادى خالص عند الحفظ (التقارير القديمة بتفضل
  زي ما هي). الواجهة بقت تعرض: آخر تقرير مستورد (زي الأول بالظبط)، سكشن
  "إجمالي كل التقارير" (كل الدفعات مع بعض من أول استيراد)، مقارنة تفاعلية
  بين أي دفعتين (إنفاق/وصول/نقرات + الفرق)، وجدول "أرشيف التقارير" (تاريخ
  كل استيراد وعدد حملاته وإجمالياته).
- `setup.sql` قسم ٢١ (نفس القسم فوق): `ad_campaigns.report_batch_id` +
  index عليه.
- **لازم `setup.sql` قسم ٢١ يتشغّل في Supabase SQL Editor** قبل ما أي من
  الفيتشرين دول يشتغلوا فعلياً (constraint الحالة الجديدة + الأعمدة
  الجديدة)، ولازم Edge Functions التالتة اللي اتغيّروا/اتضافوا تتنشر يدوياً:
  `leads-bulk-create` (تحديث)، `leads-list` (تحديث)، `leads-complete-missing-data`
  (جديدة).
- بصمة الكاش اترفعت لـ `?v=38` في `index.html`.

## تحويل مريض لطبيب آخر → ليد جديد + شاشة "تحويلات الأطباء" (`?v=43` — ٢٠٢٦-٠٨-٣٠)

- طلب المستخدم: چيكبوكس "محوّل لطبيب آخر" داخل فورم الزيارة (تعديل/إضافة)
  في أرشيف المرضى — عند تفعيله يُكتب اسم الطبيب المحوّل له، ويتحوّل المريض
  تلقائياً كـ"ليد" جديد لشاشة الليدز عشان الريسبشن/خدمة العملاء تحجز له
  معاد جديد. كل الليدز دي بتاخد بادچ/مصدر واحد بيوضح إنها تحويل من طبيب
  العيادة، ومعاها فلتر/شاشة مستقلة زي أرشيف الليدز بالظبط.
- `setup.sql` قسم ٢٣: عمودين جداد `patient_visits.referred_to_other_doctor`/
  `referred_doctor_name`، قيمة مصدر جديدة `doctor_referral` في
  `leads_source_check`، ودالة `SECURITY DEFINER` جديدة
  `create_doctor_referral_lead(patient_id, doctor_name)` — لازمة لأن مين
  بيعدّل الزيارات (تمريض/أرشيف كامل) مش بالضرورة عنده صلاحية "leads insert"
  الأساسية (مقصورة على استقبال/خدمة عملاء/مدير). الدالة بتاخد بيانات
  المريض (اسم/تليفون) وتعمل بيها ليد `source='doctor_referral'`،
  `patient_id`/`patient_type='existing'` (ربط تلقائي بالمريض من غير عمود
  إضافي)، و`message_text` فيه اسم الطبيب المحوّل له.
- `render-patients.js`: فورم الزيارة (`openVisitFormModal`) فيه الچيكبوكس
  + حقل الاسم (يظهر بس لو اتفعّل، إجباري لو اتفعّل). لو الزيارة **مكانتش**
  محوّلة قبل كده واتفعّل التحويل دلوقتي، بينادي `createDoctorReferralLead`
  بعد حفظ الزيارة — لو الزيارة أصلاً محوّلة (وضع تعديل) مبيكررش إنشاء ليد
  تاني. سجل الزيارات وتفاصيل الزيارة بيوضحوا "محوّل لـ..." لو موجود.
- `render-leads.js`: `SOURCE_LABELS` فيها القيمة الجديدة "تحويل من طبيب
  العيادة" (مستبعدة عمداً من دروب داون "إضافة ليد يدوي" — مالهاش معنى
  يدوياً)، وشاشة فرعية جديدة "تحويلات الأطباء" (`renderReferralsScreen`) —
  نسخة من أرشيف الليدز بس مفلترة على `source=doctor_referral` ثابت، بنفس
  فلاتر الحالة/الموظف (آخر رد).
- `leads-list` Edge Function: بارامتر جديد `source` (فلتر مباشر على عمود
  `leads.source`) — مستخدم من شاشة "تحويلات الأطباء" الجديدة.
- `db.js`: دالة جديدة `createDoctorReferralLead` (RPC).
- بصمة الكاش اترفعت لـ `?v=43` في `index.html` (db.js/render-patients.js/
  render-leads.js).
- **لازم**: تشغيل قسم ٢٣ من `setup.sql` في Supabase SQL Editor، ونشر
  `leads-list` Edge Function المحدّثة.

## التمريض بقى تشوف ملفات الأرشيف (قراءة فقط) + طباعة الملفات (`?v=45` — ٢٠٢٦-٠٨-٣٠)

- بلاغ المستخدم: التمريض معندهاش وصول فعلي لتاب "أرشيف المرضى" رغم إنها
  الشاشة الافتراضية ليها (`defaultTab`) — السبب: `canSeeTab("patients")`
  في `roles.js` كان بيشترط `has_archive_access`/`has_archive_view_only`/
  `super_admin` بس، ورول `nursing` المجرد مش من ضمنهم، فالتاب كان بيتخبّى
  عنها تماماً. نفس القصور موجود في `patient-files-list`/
  `patient-files-download` Edge Functions (شرط `allowed`). الحل: أضيف
  `nursing` كشرط قراءة إضافي في التلاتة أماكن (بدون أي تعديل على
  `has_archive_access` نفسه) — التمريض بتشوف/تتصفح/تنزّل الملفات زي أي
  حد عنده أرشيف كامل، لكن من غير رفع/تعديل/حذف (زرارات دي لسه مقصورة على
  `canUpload()` في الفرونت‌إند و`has_archive_access` في باقي الـ Edge
  Functions زي الأول تماماً).
- **طباعة الملفات**: زرار "🖨 طباعة" لكل ملف على حدة + زرار "🖨 طباعة كل
  الملفات" أعلى قسم الملفات في مودال المريض — بيفتحوا تاب جديد فيه كل
  ملف (صورة/PDF) في صفحة مستقلة (`page-break-after`) وبينادي
  `window.print()` تلقائي. مفيش SQL/Edge Function جديدة — بيستخدم
  `downloadPatientFile` الموجودة بالفعل، بس بيعرض الملف بدل ما ينزّله.
- بصمة الكاش اترفعت لـ `?v=45` في `index.html` (roles.js/render-patients.js).
- **لازم**: نشر `patient-files-list` و`patient-files-download` Edge
  Functions يدوياً على Supabase (مفيش تعديل SQL مطلوب).

## عنوان/تاريخ لكل مستند في طباعة البروفايل + فوتر مخصص (`?v=51` — ٢٠٢٦-٠٨-٣٠)

- طلب المستخدم بعد معاينة طباعة بروفايل المريض: كل صفحة مستند تبقى معنونة
  باسم القسم اللي اتربع فيه (زي "تحقيق شخصية"/"تأمين"/"الأشعة"...) وبتاريخه،
  وطلب صراحة إضافة **تاريخ إصدار المستند** كحقل جديد يُدخل وقت الرفع (مختلف
  عن `uploaded_at` اللي هو وقت الرفع على النظام نفسه) — وكمان استخدام خطوط
  الهوية داخل كل الملف، وتغيير فوتر المتصفح اللي كان شايل "about:blank".
- `setup.sql` قسم ٢٥: عمود اختياري `patient_files.issued_at date`.
- `patient-files-upload`/`patient-files-list` (Edge Functions): قراءة/تخزين/
  إرجاع `issued_at` (تحقق بسيط من صيغة `YYYY-MM-DD`، اختياري تماماً).
- `render-patients.js`: خانة "تاريخ إصدار المستند" جديدة في نموذجي الرفع
  الاتنين (شاشة "الاستقبال" + زرار "+ رفع" لكل تصنيف في مودال المريض) —
  بتتبعت كـ`issued_at` في الـ FormData لو اتملت.
  دالة جديدة `filePrintHeaderHtml(it)` بتبني شريط عنوان أعلى كل صفحة مستند
  مطبوعة (اسم القسم عن طريق `categoryLabel()` + التاريخ — `issued_at` لو
  موجود وإلا `uploaded_at`) — مستخدمة في `printPatientFiles` و
  `printPatientProfile` الاتنين لتوحيد الشكل.
  ثابت جديد `PRINT_FOOTER_HTML`: عنصر `position:fixed;bottom:0` بنص "التقرير
  صادر من مركز عيادات سونو التخصصية"، مُضاف مرة واحدة في `<body>` وبيتكرر
  تلقائياً في أسفل كل صفحة مطبوعة (سلوك موثّق لعناصر `fixed` عند الطباعة في
  Chrome). **ملحوظة مهمة اتقالت للمستخدم**: فوتر/هيدر المتصفح الافتراضي
  (اللي فيه "about:blank"/الرابط/رقم الصفحة) إعداد داخل نافذة الطباعة نفسها
  ("Headers and footers" toggle) — مش جزء من محتوى الصفحة، فمش قابل للتحكم
  فيه من الكود مباشرة؛ العنصر الجديد ده إضافة داخل الصفحة، مش استبدال لشكل
  المتصفح، والمستخدم يقدر يقفل هيدر/فوتر المتصفح بنفسه من نافذة الطباعة لو
  عايز الشكل نضيف تماماً.
- بصمة الكاش اترفعت لـ `?v=51` في `index.html` (render-patients.js فقط).
- **لازم**: تشغيل قسم ٢٥ من `setup.sql` في Supabase SQL Editor، ونشر
  `patient-files-upload` و`patient-files-list` Edge Functions يدوياً.

## زرار حذف بروفايل المريض في شاشة "تصفح وفلترة" — سوبر أدمن بس (`?v=52` — ٢٠٢٦-٠٨-٣٠)

- طلب المستخدم: زرار "حذف" لبروفايل المريض في جدول "تصفح وفلترة"، بصلاحية
  السوبر أدمن بس (مش أي حد عنده أرشيف كامل).
- `render-patients.js → renderBrowseScreen`: زرار "حذف" جديد لكل صف، ظاهر
  بس لو `window.SSMPDRoles.hasRole(me, "super_admin")` — بتأكيد
  `window.confirm()` (نفس نمط زرار الحذف الموجود بالفعل في صندوق "ملف
  جديد" بشاشة الاستقبال)، وبينادي `deletePatientRecord` الموجودة أصلاً في
  `db.js`. نفس معالجة خطأ الـ foreign key (لو فيه ليدز مرتبطة بالمريض)
  زي الزرار المطابق. مفيش تعديل SQL/Edge Function مطلوب — الصلاحية
  وسياسة الحذف على `patients` موجودين بالفعل (مقصورة على `has_archive_access
  أو can_manage_all_content` في RLS، والسوبر أدمن أصلاً جواها).
- بصمة الكاش اترفعت لـ `?v=52` في `index.html` (render-patients.js فقط).

## توضيح رسالة فشل حذف مستخدم نهائياً (`?v=42` — ٢٠٢٦-٠٨-٣٠)

- بلاغ المستخدم: زرار "حذف" لمستخدم (مصمم) في لوحة "المستخدمون والصلاحيات"
  رفض ينفّذ. اتأكد إن الصلاحية سليمة 100%: سياسة `"super writes all"` على
  `admins` (بتغطي `delete` كمان لأنها `for all`) بتستخدم `is_super()`
  بشكل صحيح ومفيش أي تغيير مطلوب فيها.
- **السبب الحقيقي مش صلاحية — قيد foreign key**: أي مستخدم ليه سجلات
  مرتبطة بيه (مادة محتوى `created_by`/`assigned_designer`، تعليق
  `author_id`، ملف مريض `uploaded_by`، إلخ) الأعمدة دي **من غير `on delete
  cascade`** عمداً (بعكس `admin_extra_roles`/`patient_doctor_assignments`
  اللي ليهم cascade) — عشان نحافظ على سجل "مين عمل إيه" في تاريخ النظام
  حتى لو الموظف اتشال. فحذف مستخدم ليه أي نشاط سابق (زي مصمم رفع تصميمات
  أو مادة محتوى باسمه) هيفشل برسالة Postgres خام `foreign key violation`.
  **مش باگ — تصميم مقصود**، والحل العملي هو زرار "إيقاف" الموجود بالفعل
  (بيمنع الدخول من غير ما يمسح أي سجل).
- **التحسين المنفّذ**: `render-admin.js` — رسالة الخطأ عند فشل الحذف بقت
  توضح السبب فعلياً ("ليه سجلات مرتبطة... استخدم زرار إيقاف بدل الحذف")
  بدل رسالة Postgres التقنية الخام، بنفس نمط معالجة نفس المشكلة الموجودة
  أصلاً لحذف المرضى.
- بصمة الكاش اترفعت لـ `?v=42` في `index.html` (render-admin.js فقط).

## تقرير الاستخدام: سجل جلسات الدخول + سجل الأنشطة المهمة (`?v=53` — ٢٠٢٦-٠٨-٣٠)

- طلب المستخدم: تقرير في لوحة التحكم يوضح كل مرة مستخدم بيستخدم الداشبورد،
  اسم التقرير/الملف اللي رفعه، والوقت اللي استغرقه. اتوضح بسؤال متابعة إن
  المقصود "كل نشاط مهم" (مش استيراد ملفات بس) وإن المدة بتتحسب من الدخول
  لحد الخروج (تسجيل جلسات كامل).
- `setup.sql` قسم ٢٦: جدولين جداد —
  - `login_sessions` (admin_id, login_at, last_seen_at, logout_at): جلسة
    واحدة = من تحميل الداشبورد لحد خروج صريح أو آخر "نبضة" (heartbeat).
  - `usage_activity_log` (admin_id, action_type, report_name, created_at):
    سجل عام لأي عملية رفع/إنشاء مهمة.
  RLS: كل مستخدم يقدر يسجّل بس صفوفه هو (`admin_id = my_admin_id()`)،
  والقراءة مقصورة على `is_super() أو can_manage_all_content()`.
- `db.js`: `startLoginSession`/`touchLoginSession`/`endLoginSession`
  (جداول `login_sessions`) و`logUsageActivity`/`listLoginSessions`/
  `listUsageActivity` (`usage_activity_log`).
- `app.js`: عند كل تحميل ناجح للداشبورد (`bootAfterAuth`) بيتسجّل صف جلسة
  جديد (`startUsageSession`)، ومعاه `setInterval` كل دقيقتين بيحدّث
  `last_seen_at` (heartbeat) — عشان لو المستخدم قفل التاب من غير ما يعمل
  "خروج" يفضل عندنا تقريب معقول لآخر وقت كان شغال فيه (المدة بتتحسب من
  `logout_at` لو موجود، وإلا `last_seen_at`). زرار "خروج" بقى يسجّل
  `logout_at` فعلي (best-effort) قبل ما يعمل `signOut`.
  **ملحوظة معروفة**: قفل التاب مباشرة (من غير زرار خروج) مش بيسجّل
  `logout_at` فعلي — بيعتمد على آخر heartbeat (تقريب لحد دقيقتين)، لأن
  المتصفح مش بيسمح بنداء API موثّق (Authorization header) وقت إغلاق التاب.
- بصمة الكاش اترفعت لـ `?v=53` (render-patients.js/render-summary.js
  `?v=48`/render-production.js `?v=48`/render-design.js `?v=42`/
  render-leads.js `?v=51`/render-admin.js `?v=43`/app.js `?v=42`/
  db.js `?v=44`).
- **أنشطة بتتسجّل حالياً في سجل الأنشطة**: إنشاء مادة محتوى، رفع تصميم،
  استيراد تقرير حملات إعلانات (Meta Ads)، إدخال/رفع بيانات أسبوع، رفع
  مستند مريض (الشاشتين)، رفع إكسيل ليدز جماعي، رفع فاتورة حجز. مفيش تسجيل
  لعمليات التعديل/الاعتماد/التغييرات الأصغر (نفس فلسفة `activity_log`
  الموجود أصلاً لمواد المحتوى، بس ده جدول عام عبر كل الموديولات) — لو
  حبيتوا أنشطة تانية تتضاف لاحقاً، سهل نضيفها بنفس النمط
  (`logUsageActivity(adminId, "النوع", "اسم التقرير")`).
- **لازم**: تشغيل قسم ٢٦ من `setup.sql` في Supabase SQL Editor (لا يوجد
  Edge Function متأثرة).

## استقلالية الإيميل بالكامل: إلغاء "نسيت كلمة السر" عن طريق Supabase (`?v=43` — ٢٠٢٦-٠٨-٣٠)

- **مرحلة ١ من خطة تحسينات مطلوبة من المستخدم (٦ مراحل، بيتنفذوا بالترتيب)**:
  استقلالية الإيميل → لوحة نشاط الفريق → تقرير أداء موحّد → تنبيهات SLA →
  لوحة تحكم مركزية → بحث موحّد/اختصارات كيبورد.
- طلب المستخدم صراحة: عدم الاعتماد على Supabase للإيميل نهائياً — لا تأكيد
  تسجيل (متوقف بالفعل من قبل) ولا "نسيت كلمة السر" (كان بيستخدم نفس خدمة
  الإيميل المجانية المحدودة ٢-٤ إيميلات/ساعة). التحكم في الباسوورد لازم
  يبقى بالكامل عن طريق السوبر أدمن.
- **التنفيذ**: مودال "نسيت كلمة السر؟" في شاشة الدخول (`app.js →
  openForgotPasswordModal`) اتبسّط بالكامل — بدل فورم إيميل + نداء
  `Auth.resetPasswordForEmail()` (Supabase)، بقى رسالة ثابتة توضح إن
  الباسوورد بيتغيّر بس عن طريق السوبر أدمن (زرار "تغيير كلمة السر" لأي
  مستخدم، موجود بالفعل في لوحة "المستخدمون والصلاحيات" من `?v=35`).
  الرابط نفسه ("نسيت كلمة السر؟") فضل ظاهر في شاشة الدخول — بيفتح المودال
  الجديد بس.
- **مفيش تغيير مطلوب في `auth.js`/`setup.sql`**: دالة `resetPasswordForEmail`
  في `auth.js` والمستمع لحدث `PASSWORD_RECOVERY` في `app.js` اتسابوا زي
  ما هم (كود ميت غير مُستخدم، مش خطر) — الأولوية كانت لأقل تعديل ممكن
  يحقق الهدف (تقليل الكوتا المستخدمة)، وممكن ينضفوا لاحقاً لو حبينا.
- بصمة الكاش اترفعت لـ `?v=43` في `index.html` (app.js فقط).

## لوحة نشاط الفريق داخل تقرير الاستخدام (`?v=44` — مرحلة ٢ — ٢٠٢٦-٠٨-٣٠)

- استكمالاً لخطة التحسينات (٦ مراحل، مرحلة ٢ من ٦): سكشن "تقرير الاستخدام" في
  لوحة "المستخدمون والصلاحيات" كان بيعرض سجلات خام بس (كل جلسة/نشاط سطر
  لوحده) — بدون أي تلخيص. اتضاف "لوحة نشاط الفريق" فوق السجلات الخام:
  - جدول "الأكثر نشاطاً" (أعلى ٥ مستخدمين نشيطين بإجمالي وقت الاستخدام) —
    لكل واحد: عدد الجلسات، إجمالي الوقت، متوسط الجلسة، عدد الأنشطة، آخر دخول.
  - قائمة "مستخدمين مفعّلين بس مسجّلش أي جلسة" (ضمن آخر ٢٠٠ جلسة المحمّلة) —
    بتساعد المدير يلاحظ فوراً مين مبيستخدمش النظام.
- **مفيش أي نداء إضافي للسيرفر** — الملخص بيتحسب بالكامل في المتصفح من نفس
  الـ٢٠٠ جلسة/نشاط اللي أصلاً بيتحمّلوا عند الضغط على "تحميل تقرير الاستخدام"
  (زرار on-demand موجود بالفعل من `?v=53` السابقة) — بند "أقل كوتا ممكنة"
  محفوظ بعدم إضافة أي جدول/RPC/Edge Function جديدة.
- `render-admin.js`: دوال جديدة `sessionDurationMins`، `fmtMinsPlain`،
  `renderUsageSummaryHtml` — الأخيرة بتاخد `admins` (متاحة أصلاً في `render()`)
  عشان تربط كل جلسة/نشاط بصاحبه وتحسب الإجماليات، وتفلتر على المستخدمين
  النشطين (`active=true`) بس.
- بصمة الكاش اترفعت لـ `?v=44` في `index.html` (render-admin.js فقط).

## تقرير أداء موحّد للموظفين (`?v=45` — مرحلة ٣ — ٢٠٢٦-٠٨-٣١)

- استكمالاً لخطة التحسينات (مرحلة ٣ من ٦): سكشن جديد "أداء الموظفين" في لوحة
  "المستخدمون والصلاحيات"، فوق سكشن "تقرير الاستخدام" مباشرة — بيجمع إنتاجية
  كل موظف في موديول المحتوى (إنشاء/تصميم/نشر) في جدول واحد، بدل ما يتجمعوا
  يدوياً من شاشات متفرقة.
- **تحميل عند الطلب** (زرار "تحميل تقرير الأداء") — نفس نمط تقرير الاستخدام،
  نداء واحد بس لـ`listContentItems()` (بترجع كل `content_items`، متاحة
  لأي موظف نشط حسب RLS الموجودة أصلاً) والتجميع بيتم بالكامل في المتصفح —
  مفيش أي جدول/RPC/Edge Function جديدة (بند "أقل كوتا ممكنة" محفوظ).
- الأعمدة: مواد أنشأها (`created_by`)، مواد صممها/استلمها (`assigned_designer`
  مع `design_received_at` مسجّل)، مواد نشرها (`published_by`) — مرتبة تنازلياً
  بإجمالي النشاط.
- **مؤشرات موديول الليدز (الدخل/الحجوزات حسب الموظف) اتسابت في مكانها**
  (تاب "داشبورد الإدارة" داخل موديول إدارة الليدز، من `?v=38`) بدل تكرارها
  هنا — إشارة نصية في أسفل الجدول الجديد توجّه لمكانها.
- `render-admin.js`: دالة جديدة `renderPerformanceReportHtml(items, admins)`.
- بصمة الكاش اترفعت لـ `?v=45` في `index.html` (render-admin.js فقط).

## تنبيهات SLA — مواد متأخرة في الاعتماد + ليدز محتاجة متابعة عاجلة (`?v=48`/`?v=52` — مرحلة ٤ — ٢٠٢٦-٠٨-٣١)

- استكمالاً لخطة التحسينات (مرحلة ٤ من ٦): أول تنبيهات فعلية في النظام —
  قبل كده كل حاجة كانت "pull" (لازم حد يفتح الشاشة يدوّر). دلوقتي فيه ٢
  تنبيه بصري (بانر أحمر) بيظهر تلقائياً في الشاشات اللي أصلاً بيفتحها
  المسؤولون، من غير أي إعداد إضافي:
  - **"مواد متأخرة في الاعتماد"** في شاشة "إدارة المحتوى" (`render-review.js`):
    مواد في مرحلة `initial_approval`/`final_approval` من غير حركة لأكتر من
    ٤٨ ساعة (بيتحسب من `content_items.updated_at`، بيتحدّث تلقائياً مع أي
    تغيير بما فيها انتقال المرحلة). **مفيش أي نداء إضافي للسيرفر** — بيتحسب
    من نفس الـ`items` المُحمّلة بالفعل لعرض الـKanban.
  - **"ليدز محتاجة متابعة عاجلة"** في داشبورد إدارة الليدز (`render-leads.js
    → renderDashboardScreen`): ليدز بحالة `new` (يعني لسه من غير أي محاولة
    تواصل — الحالة بترقّي تلقائياً لـ`in_progress` عند أول محاولة) من غير
    رد لأكتر من ٢٤ ساعة. ده محتاج نداء إضافي واحد (`listLeads({status:"new"})`)
    جنب نداء الإحصائيات الموجود بالفعل — قبلناه لأنه مرة واحدة بس عند فتح
    الداشبورد، مش polling متكرر.
- **قرار مقصود**: مفيش إشعارات push/إيميل حقيقية — بما إن المستخدم لسه
  طلب في المرحلة ١ إلغاء الاعتماد على إيميل Supabase تماماً، التنبيهات دي
  مرئية جوه الشاشات اللي المسؤولون بيفتحوها أصلاً بدل أي قناة خارجية.
- بصمة الكاش اترفعت لـ `render-review.js?v=48` و`render-leads.js?v=52` في
  `index.html`.

## نظرة عامة على النظام — لوحة تحكم مركزية للمدير/السوبر أدمن (`?v=49` — مرحلة ٥ — ٢٠٢٦-٠٨-٣١)

- استكمالاً لخطة التحسينات (مرحلة ٥ من ٦، وردّ مباشر على سؤال المستخدم
  "ليه مفيش إشعار للأدمن كمان علشان يبقي متابع؟"): سكشن جديد "نظرة عامة
  على النظام" أعلى شاشة "الملخص العام" (`render-summary.js`) — أول شاشة
  بيشوفها أي حد بعد الدخول — بس ظاهر لـ`general_manager`/`super_admin`.
  بيجمع نبضة واحدة عبر الموديولات التلاتة (محتوى/ليدز/أرشيف مرضى) بدل ما
  المدير يحتاج يفتح كل شاشة لوحده يتابعها:
  - ٣ KPI: مواد قيد الاعتماد، ليدز مفتوحة، ملفات مرضى قيد المراجعة.
  - **نفس تنبيهي SLA** من المرحلة ٤ (مواد متأخرة في الاعتماد + ليدز من غير
    رد) بس مجمّعين هنا في مكان واحد — ده الرد العملي على سؤال المستخدم:
    التنبيهات مبقتش محتاجة زيارة كل شاشة على حدة، أول ما المدير يفتح
    الداشبورد بيشوفها فوراً في "الملخص العام".
  - لو مفيش تنبيهات: رسالة إيجابية "✓ مفيش تنبيهات متأخرة دلوقتي".
- **التكلفة**: ٣ نداءات إضافية للسيرفر (`getLeadsStats`, `listLeads`
  للليدز الجديدة، `getPatientArchiveStats`) — بس مقصورة على
  `general_manager`/`super_admin` فقط (باقي الأدوار صفر تكلفة إضافية،
  زي ما كانت بالظبط). نداء ملفات المرضى ملفوف بـ`.catch()` صامت لأن
  `general_manager` ممكن ماعندوش صلاحية أرشيف مرضى أصلاً (403 متوقع ومقبول).
- **قرار مقصود**: لسه مفيش إشعارات push/إيميل حقيقية (متسق مع قرار المرحلة
  ١) — الحل هنا "سحب نشط" (المدير بيشوف التنبيهات أول ما يفتح الداشبورد)
  مش "دفع" (push فعلي)، وده بيغطي عملياً نفس الهدف (المدير يفضل متابع) من
  غير أي بنية تحتية إشعارات جديدة.
- بصمة الكاش اترفعت لـ `?v=49` في `index.html` (render-summary.js فقط).

## بحث موحّد + اختصار Ctrl+K في الشريط العلوي (`?v=44`/`?v=45`/`?v=49`/`?v=53`/`?v=54` — مرحلة ٦ (الأخيرة) — ٢٠٢٦-٠٨-٣١)

- استكمالاً لخطة التحسينات (مرحلة ٦ من ٦، آخر مرحلة): صندوق بحث واحد في
  الشريط العلوي (بين اللوجو واسم المستخدم) — بيبحث في المحتوى/الليدز/
  المرضى مرة واحدة حسب صلاحيات المستخدم الفعلية، بدل ما يفتح كل شاشة
  ويبحث فيها لوحده.
- **الاستخدام**: `Ctrl+K`/`Cmd+K` من أي مكان في الداشبورد يفوكس على
  الصندوق، كتابة كلمتين على الأقل + Enter يشغّل البحث، `Esc` يقفل النتائج.
  الضغط على نتيجة "محتوى" بيفتح مودال المراجعة/التفاصيل بتاعتها مباشرة
  (`render-review.js`)، والضغط على نتيجة "ليدز"/"مرضى" بينقّل لشاشة
  "أرشيف الليدز"/"تصفح وفلترة" وهي متفلترة بنفس كلمة البحث تلقائياً.
- **صفر تكلفة إضافية لمين مالوش وصول**: البحث بيستخدم `Promise.all` بس
  بيبعت نداء الموديول اللي المستخدم أصلاً عنده تاب ليه (`tabs` array) —
  موظف مالوش تاب "الليدز" مثلاً، نداء بحث الليدز مش بيتبعتش خالص له.
- **مفيش نداءات جديدة للسيرفر بمعنى الكلمة** — البحث بيستخدم نفس الاستعلامات
  الموجودة بالفعل: `db.js → searchContentItems(term)` (استعلام مباشر جديد
  بس بسيط على `content_items`، بديل لـ`listContentItems()` المستخدمة أصلاً
  لغرض تاني)، و`leads-list`/`patient-files-list` Edge Functions المستخدمين
  فعلياً (بارامتر `search` كان موجود فيهم بالفعل من غير أي تعديل).
- **آلية فتح النتيجة داخل موديول لسه ما اتحمّلش**: (أ) لنتائج المحتوى —
  متغيّر عالمي مؤقت `window.SSMPDPendingOpenContentId` بيتحط قبل
  `switchTab("review")`، وبيتقرا ويتصفّر جوه `render-review.js` بعد ما
  البيانات تخلص تحميل، لو الآيدي موجود يفتح مودال المراجعة تلقائي. (ب)
  لنتائج الليدز/المرضى — الموديولين عندهم بالفعل حقول بحث/فلترة داخلية،
  فبدل تكرار المنطق، اتضاف `openSearch(term)` على `window.SSMPDRenderLeads`/
  `window.SSMPDRenderPatients` بيهيّئ حالة الشاشة (التاب الفرعي + قيمة
  البحث) قبل ما `switchTab()` يرسم الشاشة، فتظهر جاهزة ومفلترة من غير أي
  خطوة يدوية من المستخدم.
- بصمة الكاش اترفعت لـ `db.js?v=45`، `render-review.js?v=49`،
  `render-patients.js?v=54`، `render-leads.js?v=53`، `app.js?v=44` في
  `index.html`.
- **مفيش تعديل SQL/Edge Function مطلوب** — كل الاستعلامات المستخدمة
  موجودة ومنشورة بالفعل.

## اكتمال خطة التحسينات الستة (٢٠٢٦-٠٨-٣١)

كل المراحل الستة اللي اقترحتها كنصيحة خبير (استقلالية الإيميل، لوحة نشاط
الفريق، تقرير أداء موحّد، تنبيهات SLA، لوحة تحكم مركزية، بحث موحّد) خلصت
ونُشرت فعلياً. مفيش مراحل تانية معلّقة من الخطة دي — أي شغل جديد يبقى
طلب مستقل.

## اكتمال باقي اقتراحات المراجعة الثانية: مصمم بالرول الإضافي + حدود SLA
## قابلة للتعديل + تصدير Excel لتقرير الأداء (`?v=46`/`?v=50`/`?v=54` —
## ٢٠٢٦-٠٨-٣١)

استكمالاً للمراجعة الثانية للداشبورد (فجوات معروفة كانت مؤجّلة توفيراً
للكوتا)، نُفّذت هذه الدفعة:

- **دروب داون "اختيار المصمم"/"تغيير المصمم المسؤول" بقى يشمل الرول
  الإضافي مش الأساسي بس**: دالة جديدة `list_designers_all()` (نفس نمط
  `list_active_sono_doctors()`/`list_admins_basic()`) بترجع أي موظف نشط
  عنده `designer` أساسي أو إضافي. `render-review.js` بقى يستخدمها
  (`db.js → listDesignersAll`) مع fallback على الفلترة القديمة لو الدالة
  لسه مش منشورة على القاعدة الحية.
- **حدود SLA (٤٨ ساعة للمحتوى، ٢٤ ساعة لليدز) بقت قابلة للتعديل**، مش
  أرقام ثابتة في الكود: جدول جديد `app_settings` (صف واحد id=1) —
  `content_sla_hours`/`leads_sla_hours`. سكشن جديد "إعدادات التنبيهات
  (SLA)" في لوحة "المستخدمون والصلاحيات" (سوبر أدمن) لتعديلهم. الشاشات
  التلاتة اللي بتحسب تنبيهات SLA (`render-review.js`، `render-leads.js
  → renderDashboardScreen`، `render-summary.js` نظرة عامة النظام) بقت
  تقرا الحد من `app_settings` (نداء واحد إضافي، ملفوف بـ`.catch` fallback
  للقيمة الافتراضية لو الجدول لسه مش منشور).
- **تصدير Excel لتقرير الأداء الموحّد** في لوحة الأدمن — زرار "⬇ تصدير
  Excel" فوق الجدول، بيستخدم مكتبة SheetJS المُحمّلة بالفعل في
  `index.html` لموديول الليدز (`XLSX.utils.aoa_to_sheet`) من غير أي
  مكتبة/نداء إضافي. (تصدير Excel/PDF لداشبورد الليدز كان أصلاً موجود من
  دفعة سابقة — `exportDashboardExcel`/`exportDashboardPdf`.)
- بصمة الكاش اترفعت لـ `db.js?v=46`، `render-review.js?v=50`،
  `render-leads.js?v=54`، `render-summary.js?v=50`، `render-admin.js?v=46`
  في `index.html`.
- **لازم**: تشغيل الـ SQL الجديد آخر `setup.sql` (دالة `list_designers_all`
  + جدول `app_settings` وسياساته) في Supabase SQL Editor — القسم الجديد
  بس، مش الملف كامل (نفس مشكلة `admins_role_check` القديمة الموثّقة).

**فجوات مؤجّلة عمداً (طلب المستخدم "نفذهم كلهم" بس دول محتاجين قرار/مدخل
منه شخصياً مش تنفيذ كود)**:
- **SMTP مخصص (Resend)**: تنفيذه فعلياً بيحتاج مفتاح API من حساب Resend
  الخاص بالمستخدم — دي بيانات اعتماد (credential) ممنوع إدخالها من الشات
  نيابة عنه لأي سبب. **كمان**: تفعيل SMTP بيرجّع الاعتماد على قناة إيميل
  حقيقية، وده بيتعارض مع قرار المستخدم الصريح في المرحلة ١ ("مش عايز
  اعتمد على Supabase للإيميل نهائياً") — القرار الحالي (تحكم كامل بالباسورد
  عن طريق السوبر أدمن) قائم على طلبه المباشر ده. لو غيّر رأيه وعايز يرجّع
  قناة إيميل، محتاج يأكد ده صراحة + يجهّز مفتاح Resend ويدخله بنفسه في
  Supabase Dashboard → Auth → SMTP Settings (خطوات بالعربي جاهزة لو طلبها).
- **اختبار على أجهزة موبايل حقيقية**: مفيش أجهزة فعلية متاحة في بيئة
  التنفيذ دي — أقصى تحقق ممكن هو فحص المتصفح الحقيقي (Chromium) اللي
  اتعمل بعد النشر (تحميل الصفحة، فحص الكونسول من غير أخطاء) — ده اتعمل،
  لكنه مش بديل لاختبار حقيقي على موبايل/تابلت فعلي بشبكة الفريق.
