const fs=require('fs'),assert=require('node:assert/strict'),{JSDOM}=require('jsdom');
const src=fs.readFileSync('assets/js/render-patients.js','utf8'),tick=()=>new Promise(r=>setTimeout(r,0));
(async()=>{
 const dom=new JSDOM('<details class="nutrition-visit-card" open><div id="status"></div></details><div id="meals"><div class="meal-row" data-meal-id="m"></div></div>',{runScripts:'outside-only',pretendToBeVisual:true}),w=dom.window;let timers=[],reads=0,today;
 w.setTimeout=f=>timers.push(f);w.eval(fs.readFileSync('assets/js/nutrition-view.js','utf8'));today=w.SwnwNutritionView.today();let yesterday=w.SwnwNutritionView.shiftDay(today,-1),rows=[{meal_id:'m',meal_name_snapshot:'فطار قديم',tracking_date:today,completed:true,completed_at:new Date().toISOString(),recorded_by_account_id:'account'},{meal_id:'old',meal_name_snapshot:'وجبة محفوظة',tracking_date:yesterday,completed:true,completed_at:new Date().toISOString(),recorded_by_admin_id:'admin'}],legacyRows=[{meal_id:'m',completed:true,completed_at:new Date(yesterday+'T10:00:00Z').toISOString(),recorded_by_account_id:'account'}];
 w.SSMPDDb={listNutritionMealCompletions:async()=>legacyRows,listNutritionDailyCompletionHistory:async(id,from,to)=>{reads++;assert.equal(id,'v');assert.equal(to,today);return rows}};w.escapeHtml=v=>String(v).replace(/</g,'&lt;');
 w.eval(src.slice(src.indexOf('  function watchNutritionStatus('),src.indexOf('  function openNutritionVisitFormModal(')));
 const el=w.document.getElementById('status');w.watchNutritionStatus(el,{id:'v',visit_date:'2026-01-01',meals:[{id:'m',name:'<img>'}]});await tick();assert.match(el.textContent,/سجل متابعة الوجبات/);assert.match(el.textContent,/المريض/);assert.match(el.textContent,/الموظف/);assert.match(el.textContent,/تأكيد سابق/);assert.match(el.textContent,/وجبة محفوظة/);assert.match(el.textContent,/اسم الوجبة وقت التسجيل/);assert.equal(el.querySelector('img'),null);assert.equal(el.querySelectorAll('tbody tr').length,7);
 rows=[];legacyRows=[];timers.shift()();await tick();assert.match(el.textContent,/لا توجد تأكيدات/);el.remove();let before=reads;timers.shift()();await tick();assert.equal(reads,before);
 const code=src.slice(src.indexOf('    function applyCompletionUi()'),src.indexOf('    function loadCompletions()'));
 w.eval('var v={visit_date:"2026-01-01",meals:[{id:"m"}]},trackingDay={value:"2026-09-01"},mealsContainer=document.getElementById("meals"),completions={},completionsLoaded=false,completionWrites=0;'+code+';applyCompletionUi();');assert.equal(w.document.querySelector('input').disabled,true);
 w.eval('completionsLoaded=true;completions={m:{completed:true}};applyCompletionUi();');assert.equal(w.document.querySelector('input').checked,true);assert.equal(w.document.querySelector('input').disabled,false);
 w.eval('completions={m:{completed:false}};applyCompletionUi();');assert.equal(w.document.querySelector('input').checked,false);assert.equal(w.document.querySelectorAll('input').length,1);
 dom.window.close();console.log('PASS dashboard nutrition: daily history, actors, snapshots, empty days, escaping, polling cleanup');
})().catch(e=>{console.error(e);process.exitCode=1});
